# Orpah Demo 路线图 / 待办清单

> 按「Orpah 协议目标 + 物联网演示系统」两条线梳理的缺口清单。
> 记录于 2026-09-11。逐步实现，优先级 P0 > P1 > P2。
> 2026-09-11 补充：走失案件闭环、一个走失者多客户端、隐私/时钟、无认证防 spoof 优先级上调。
> 完整上下文见 `/memories/orpah-demo-roadmap.md`。

## 〇、范围原则（最高约束，2026-09-11 用户定）

> **ORPAH 是技术搜寻手段；demo 项目边界应取最小。**

- **只做「用无线技术找到人」这一段**：登记走失态 → 下发走失表 → 路由器发现上报 →
  服务器回执落库 → RSSI 定位 → 审计与告警。
  **ORPAH 不是公安办案系统**，不承载警务流程。
- **默认不做**（除非用户明确要求，且能论证它服务于「搜寻」而非「办案」）：
  组织/机构建模、角色与权限体系、警员身份、案件分配/派单/办案流程、多租户隔离。
- **判据**（每条新需求先过这一关）：
  **「让找人更快更准」→ 做；「让管理/流程更完整」→ 默认不做，先记本清单。**
- **「谁」的分层**：需要「谁」时止步于**审计标签**（自由文本 `actor`，见 §三）；
  需要「谁负责」（经办/分配）时先确认**是否存在真实运营方** —— demo 目前**无登录者**，
  自填标识不等于身份。
- **原则「孤证不立」（2026-09-13 用户定，项目原则）**：**任何单条来自机器的证据都不足以
  支撑位置判定**（设备自报 / 单台 Router 的观测都是孤证）→ 可信度只能来自**多个互不依赖的
  来源互相印证**（多观测一致性 + 离群剔除）；**无冗余时必须如实降级**（标「单一来源、未交叉校验」），
  **不得**因为「圈算得小」就当成可信（**精度 ≠ 可信度**，两者必须分开显示）。
  **唯一例外**：证据**明确来自人**（**办案人 / 搜救队**）→ 单源可采信，但必须留痕（审计 `actor`），
  人工来源优先于机器观测且不参与一致性剔除。
  规格落笔：`Protocol/docs/orpah-over-halow/SPEC.md` §8 原则 P-1。
- **已应用**（2026-09-11）：「案件经办/分配」（见 §三）与「告警处置态/时效策略」（见 §三）
  均按此原则**只记录、不实现**。

## 一、设备与身份侧（最像「产品」的缺口）

- [x] **设备清册/注册**：客户端台账——SN、所属组织、CC、**绑定走失者/对象（一对多：一个走失者可挂多个客户端，如项链/鞋/眼镜）**、状态（启用/停用/丢失/报废）、首次入网时间、最近见时间。（`registry.py` + `registry.html`，2026-09-11 完成）
- [~] **密钥/证书生命周期**（2026-09-12 主体完成；**2026-09-13 用户定：不做轮换 → 本项口径收窄**）：
  密钥生成→分发→**撤销**（**不做轮换/退役**）；ES256 公钥、HMAC 密钥与 SN 绑定；作废机制（CRL/OCSP 思路）。
  - **⚠ 口径（2026-09-13 用户定，以《Orpah ID 协议规范》§6.3.1 v1.19 为准）**：设备侧只持有
    **1 把 ECDSA P-256 私钥 + 1 把 32 字节 HMAC 密钥**，**一代终身，“不存在第 2 代”**。
    理由：① 本系统**对使用者无强制约束力** —— 觉得不好用，抛弃客户端或**物理屏蔽（包锡纸）**即可，
    不需要靠轮换应对“疑似泄露”；② **免电池取能终端做不了轮换**（轮换 = 高能耗 + 必须在线完成一次事务；
    取能设备承诺不了“换钥那一刻我有电”，即能量轴 `P=0`）。
    → 下面这些 `rotate`/`grace`/`retired` 实现**降为“对比用的演示代码，不是设备模型”**：
    **不得基于它继续加功能**，页面/文档里也不得当成系统能力。
    **✅ 已按 A 清理（2026-09-13）**：`keys.html` **移除轮换/退役入口**（只留签发/采用/撤销/恢复），
    页面说明改成“本系统不做轮换 + 理由”；`API.md` §9 把 `rotate`/`retire` 标为 ⚠ 非规范试验代码；
    代码与 `test_keys.py`（含轮换/宽限用例）**保留**，供对照与回归。
  - **已做**：`orpah_id.KeyStore` 多代密钥 + 状态机（`active`/`grace`/`retired`/`revoked`）；
    `keystore.KeyStoreDB` 落 SQLite（与清册同库，**重启不丢**）；`/api/keys` + `keys.html`
    （契约见 `API.md` §9）；每步写审计事件 `key_issue`/`key_rotate`/`key_retire`/
    `key_revoke`/`key_unrevoke`（带 `actor`，见 §三 审计）。
  - **轮换不断链**：旧钥进 grace（`ORPAH_KEY_GRACE_SEC`，默认 7 天）期内**新旧都能验签**，
    在役设备不必与服务器同时换钥；宽限到期 `sweep` → `retired`。
  - **不改报文格式**：报文头**不加 kid**，验签按代次倒序**逐代试签** → 老报文/老设备零改动；
    命中哪代回 `kid`/`gen`，并写进 `id_report` 事件的 `detail`（`gen=<n>`）；页面 / 事件历史可见
    「这条上报用的是第几代钥」。
  - **撤销不可逆**：整机作废后 `issue`/`rotate` 被拒（需先 `unrevoke` 再重新签发）；
    协议层 `_issue` 亦有同名护栏（双保险）。
  - **模拟器私钥不入库**：由 (SN, 代次) 确定派生（`derive_demo_privkey`）→ 服务器重启后
    公钥仍对得上（否则持久化的密钥库每次重启都验不过）；真实设备用 `demo_key=False`。
  - **未做（两个真缺口）**：
    1. **作废表分发（CRL/OCSP 思路）**：Router 侧目前**不知道**某 SN 已撤销，仍会转发其上报，
       由 Server 拒签（能兜住，但不是最小权限）。
       **⏸ 用户决定（2026-09-13）：往后放（本项不做）**。理由（用户提的两点 + 我核实的结论）：
       - **①「CRL 会有多大」** —— 实测（`TEMP/probe_crl_size.py`，按本条的 entries schema
         `{sn, revoked_at, reason, gen}` 生成真实 JSON）：**单条 76 B**，整表
         **1 条 131 B / 10 条 0.8 KB / 50 条 3.8 KB / 100 条 7.6 KB / 1000 条 75 KB / 10000 条 752 KB**。
         即 **~50 条就超过单个 1472 B 载荷** → 要分帧 + 断点续传/版本号；而「变更即全量」意味着
         **每撤一台就推一次整表**（1000 条撤一台上行 75 KB）。**用户的担心是对的。**
       - **②「实际用处不大」** —— 核实为**基本正确**：CRL 的两点收益里，
         **可见性那条已被 Server 兜住** —— Router 今天照常转发，所以 Server **看得到**被拒上报
         （`id_reject` 审计 + `sig_fail_rate` 告警），"有人在用已撤销设备"这条线索并没丢；
         真正剩下的收益只有**省上行空口**（不把注定被拒的报转发上去），
         只有「大量已撤销设备仍在持续发射」时才显著。**当前 demo 尺度下确实不值这个复杂度。**
       - **回头看它的触发条件**（写在这里，免得重新论证）：a) 实网里已撤销设备数量到几十~上百台
         且仍在持续上报（空口成为瓶颈）；b) 或者协议从 UDP+JSON 换成紧凑二进制（届时 76 B/条
         会小很多，分帧问题性质改变）；c) 或者出现「Router 必须自己拒绝」的合规/权限要求
         （最小权限原则）。**在那之前只保留本节记录，不写代码、不写 demo_l5.py。**
       - 契约（已定稿、留档备用）= 新报文 `ORPAH-CRL`（S→R 推）/ `ORPAH-CRL-REQ`（R→S 拉）/
         `ORPAH-REVOKED-SEEN`（R→S 安全事件），**与 `LOST-TABLE` 完全同构**（启动拉一次 + 变更推送
         + 已同步不再按条拉，照抄 L3b 踩过的洪泛坑），但**语义分开**：走失表=业务数据、
         撤销表=安全数据（混表以后难拆）。
       - **Router 行为（若将来做）** = 命中即不转发 + 本地计数 + 上报安全事件
         （选它而不是静默丢弃：「有人在用已撤销设备」本身就是安全线索）。
       - Server 仍是最终防线（Router 未同步时照样拒签）。与 §四 的「路由器侧观测不入签名」
         **是两回事**：这条管**身份还可不可信**，那条管**观测可不可信**。
       - 落点（将来）：`orpah_proto.py` + `server.py` + `router.py` + 端到端脚本 `demo_l5.py`；
         协议侧已写进 `Protocol/docs/orpah-over-halow/SPEC.md` §5.1 / F-10 / L3d。
    2. **密钥存储保护/产线烧录**：安全元件、私钥不可导出、供应链绑定 —— 属**硬件信任根**，
       按 §〇 范围原则**不在本 demo 范围**（只记录）。补充事实：demo **服务器侧本来就不存设备私钥**
       （模拟器私钥按 (SN, 代次) 派生，真机私钥在设备内），所以「服务器加密落库」对 demo 无实际收益；
       真正要保护的是**真机设备侧私钥**（安全元件 + 不可导出 + 产线注入），留待真机阶段。
- [x] **多算法策略与降级（2026-09-12 完成，按《Orpah ID 协议规范》§8 实现）**：原条目「按设备安全等级自动选 alg；
  私钥不可用/能量不足自动降级并告警」。**先读规格再动手** —— §8.1/§8.2/§8.3 已经把阶梯、选级流程、
  服务端处理写清了，所以**不自己发明策略**，只把规格落地：
  - ✅ **设备侧选级（§8.2）**：新增 `orpah_id.pick_level(se_ok, sign_ok, hmac_ok)`（**策略唯一源**，
    照 §8.2 文字流程：SE 不通→有 HMAC 则 L2 / 无则 L3；SE 通但 Slot0 签名失败→L1；正常→L0）。
    `Device.report(level=None)` 默认自动选级（全正常→L0，与旧行为一致）；
    演示入口 = 首页 Orpah ID 卡的「降级演示」下拉（`POST /api/ctl {action:"id_level"}`），
    选的是**哪个环节坏了**而不是写死 level —— 走的才是规格那条路径。
  - ✅ **服务端处理（§8.3）**：`verify_report` 新增 `degraded`（L2）/ `coverage_only`（L3）字段（server 原样带给下游）；
    新增**告警规则 `id_degraded`**：L2→warn（SE 异常）、L3→crit（设备异常，通知运维），
    同设备只一条取最高级、窗口外自动消警（`ORPAH_ALERT_ID_DEGRADED_SEC`，默认 300s）。
  - ✅ **存在性语义（§8.3 “L3 不用于人员确认”）**：新增 `orpah_id.counts_as_presence()`
    （= `accepted` 且非 `coverage_only`）作为**唯一依据**。
  - 🔴 **做这项时查出一个真 bug（已修）**：`ui_server._on_id_report` 原来**无条件** `registry.touch()`
    → 一条**验签失败**的伪造上报也刷新 `last_seen`。实测：暂停上报后静置 3.5s `last_seen=1789199499`，
    注入伪造报文后变 `1789199504` → **伪造报文能“报平安”，把「长未上报」告警永远抑住**（与防 spoof 的目的相反）。
    修后实测：暂停下注入**已通过的 L3** 与**被拒的伪造**都**不变** `last_seen`，恢复业务上报则正常变大。
  - 🔴 **同类第二个真 bug（已修）**：`alerts` 的 `sig_fail_rate` 把 `since` 传成展示用字符串
    （`rec["t"]="15:46:21"`）→ `_alert` 里 `int(since)` 抛 `ValueError` → **只要签名失败率告警一触发，
    `/api/alerts` 整个不可用**（红点里的其它告警一起看不到）。探针复现后改为 epoch 秒（`ts_eff`）。
  - 测试：新增 `test_levels.py`（§8.2 真值表 + §8.3 字段 + `counts_as_presence` + 告警规则 + 上述两个 bug 回归锁）
    → 已并入 `run_checks.py`（离线 9→10 套件）。
  - ✅ **指标面板按 level 分布/降级占比（2026-09-12 同轮补齐）**：`metrics.verify_stats` 加 `by_level` /
    `degraded{l2,l3,total}` / `degraded_ratio` / `level_unknown`，页面「签名校验」卡多一行
    「降级上报 L2 x · L3 y（占通过总数 z%）」；`test_metrics.py` 加 10 项（含“被拒报文宣称的级别不算”
    、“缺级别 ≠ L0”、“分母含无级别行 → 下界”、“无样本 → null”）。
  - 未做（用户已定：**最后做**）：**能量轴**（无源客户端唤醒周期/能量采集 → 上报频率与降级耦合，见下一条）。
- [x] **无源/低功耗客户端建模（协议约束，非仅画像）**：无源唤醒周期、能量采集状态、上报间隔——能量决定上报频率与 alg 降级；demo 加「能量轴」。
  - ✅ **已完成（2026-09-13）**。用户先问「能量轴是什么」→ 我用选择题把四个设计点定死再动手
    （单一变更原则）：**D1** 三参数单源模型（采集 `harvest_mw` / 储能 `charge_mj` / 每次上报代价 `cost_mj`）；
    **D2** 允许能量驱动降级，但**必须显式标注成因**（`degraded_reason`）并出告警；
    **D3** 「没电了」**升为一等状态**（新告警 kind + 把沉默按电量归因分叉）；
    **D4** 电量沿用**已签**上报里的 `payload.battery_mv`（不改签名预像、不新增字段）。
  - **我自己补的两条设计判断（需用户知情）**：
    ① 能量降级的**下限是 L1（HS256）**，**永不降到 L3** —— §8.3 里 L3 是 coverage-only、**不能确认人是否在场**，
       而“确认人在不在”正是 ORPAH 的目标；宁可**如实沉默**，也不发一条无法确认在场的报（已写进 SPEC §5.2 规则 E2）。
    ② 成因**不新增字段**：服务端从**两个都在签名覆盖内的事实**（级别 + 电量）推导
       `degraded_reason = "energy" | "key"`，攻击者无法把“密钥坏了”伪装成“没电了”，反之亦然。
  - 实现：`energy.py`（`plan`/`mv_of`/`drain`/`survive_s`/`sweep`/`axis`；策略表写在模块 docstring）、
    `alerts.py`（新 kind `id_energy`（低电 warn / 耗尽 crit）+ `no_report_energy`（沉默+低电 → warn，
    **不再**升级 crit）+ 阈值 `ORPAH_ALERT_ENERGY_LOW_MV/OUT_MV`）、`server.py`（记录 `battery_mv` +
    推导 `degraded_reason`）、`ui_server.py`（能量驱动 `every`/级别、`/api/energy`、`/api/status.energy`+`energy_axis`、
    UI「能量轴」卡片 + 扫描表）；`test_energy.py`（51 项）+ 告警/服务端用例。
  - **实测（默认演示参数：cost ES256=15mJ / HS256=5mJ、sleep 0.05mW、满储 2000mJ）**：
    | 采集 mW | 级别 | 间隔 s | 结果 |
    |---|---|---|---|
    | 0（无采集） | — | 无 | 沉默；`why=no_energy` |
    | 0.05 | HS256 | 无 | **采不敷出** → 沉默（`why=deficit`），倒计时=`储能/(sleep−采集)` |
    | 0.08 | HS256 | 166.7 | 只够降级（`degraded_saves`），服务端 `id_energy` warn |
    | 0.10 | ES256 | 300 | **门槛**：刚刚够每 300s 用 ES256 报一次 |
    | 0.20 | ES256 | 100 | 可用 |
    | 0.50 | ES256 | 33.3 | 门槛的头条数字 = **`min_harvest_mw ≈ 0.083 mW`** |
  - **已知的“间隔跳变”（有意保留，不是 bug）**：从 HS256 切到 ES256 的那一档，间隔会从 ~167s **跳**到 300s
    （因为换来的是更强的签名）。`test_energy.py` 第 8 节**专门锁住**“各级别内单调不增 + 恰好一处有意跳变”，
    免得以后有人把它当回归修掉。
  - **服务端归因分叉（D3 的落地）**：沉默 + 最后一条**已签**电量低 → `no_report_energy`（warn：等它取能）；
    沉默 + 电量充足 → `no_report`（30s warn → 300s **crit**：这才是该出警的）。**两类不合并**（处置相反）。
    诚实边界：设备如果**从没报过低电量**就直接死了，服务端**无从知道**，会按异常失联处理
    （所以 SPEC §5.2 里加了“SHOULD 在还能报的时候如实报低电量”）。
  - **浏览器实测（:8901，走了页面路径而非直接打 API）**：开启能量模型 → 采集 0.5mW 时状态格显示
    `ES256 / 33.3s / 净余 0.450mW / 可用`，扫描表 13 行并标出当前工作点；
    改 0.08mW 后**签名上报流立刻出现 HS256(L1)**（能量降级真的发生在报文里，不是只画在页面上）；
    把电量压到 3180mV（≤3300）后 `/api/alerts` 出 `id_energy/warn/mv=3180`，再把采集归零 →
    沉默 40s 后出 **`no_report_energy/warn/gap=40/mv=3180/sil=5633`**（而非 `no_report/crit`）→ **D3 达成**。
  - 参数标注：模块/页面/SPEC 三处都写明是**演示标定值、不是实测**；真机标定（TX-AH+CH32V203 实测采集/功耗）
    与“要不要把‘能量’写进 §8.1 条件列”列为 **SPEC F-11**（阶段二上机做）。
  - 测试：`test_energy.py` **73 项**（含边界/单调性/跳变锁/drain 边界；监听那批之后由 51 升到 73），并入 `run_checks.py`（离线 13→14 套件）。
  - ✅ **实测标定入口（2026-09-13，同一项的第二半）**：用户选「**C. 标定入口 + 只算/页面呈现**
    （不动链路）」。`energy_calib.py`（读文件 → 换算 → 标出处）+ `energy_calib.example.json`（示例，
    **明写不是实测**）。路径 `ORPAH_ENERGY_CALIB` → 缺省仓库根 `energy_calib.json`。
    - **两种写法都收**（真上机测到的是电流×电压×时长，不是 mW/mJ）：`sleep_ua`、
      `active_ma`×`report_ms` → 代码换算（`µA×mV÷1e6 = mW`、`mA×mV×ms÷1e6 = mJ`），**算式进页面**
      供核对；直接给 `sleep_mw`/`cost_mj` 也行（两种都给且差 >1% → 一条提示，不是悄悄取一个）。
    - **三条硬规则**：① 没文件 ≠ 出错（就是“未标定”，页面写清）；② 文件有问题 → **整份不采用**
      （值回演示值 + 错误可见，**绝不半份生效** —— 一半实测一半演示而页面看着“已标定”最坏）；
      ③ 逐项标出处（`measured`/`demo`）+ 探源（device/who/when/how + 文件指纹）。
    - **策略阈值不是标定项**（`min_interval_s`/`max_useful_interval_s`/`emergency_interval_s` 是产品选择
      → 列进“已忽略”）；未知键也**列出来**（不静默丢）；`_` 开头当注释。
    - 页面：徽标（未标定 / 部分实测 / 实测标定 / 文件不可用）+ 路径指纹 + 重载按钮**常显**，
      逐字段表（生效值 / 演示值 / 出处 / 算式）与长口径折进 `details.fold`。
    - **实测（示例文件 8µA/12mA×45ms）**：待机 0.05 → **0.0296 mW**、ES256 上报 15 → **1.998 mJ**
      → 门槛从 **0.083 mW 降到 0.007 mW**：演示值把上报代价估高了近 **8 倍**（所以演示参数下的
      「要多少 mW」结论不能当真）；页面上三个状态（未标定 / 实测+3 条提示 / 坏文件整份不采用）
      都实测过，窄屏 390 无横向溢出。
    - **仍未做**：真机标定数值本身（要上机测；列 F-11）；**时变取能 + 储能缓冲**与**监听/占空比开销**
      （本次用户选的是“标定入口”那条，那两个方向仍是模型形状的变更，未做）。
    - 测试：`test_energy_calib.py`（70 项）+ `test_i18n.py` 新增第三类服务端键源（40 个）+
      `test_appjs.py` 新增“开页拉整表 / 页面不写第二份字段表”守卫；离线套件 25→26。
  - ✅ **监听/占空比开销（2026-09-13，用户选 A；仍属“只算 + 页面呈现”）**：之前模型里
    **只有上报耗电、下行监听是 0**，而免电池终端必须周期醒来听（走失表/回执）—— 真机上
    **常常比待机还贵**（演示：听窗口 6 mJ/次 @60s = **0.1 mW**，而待机 0.05 mW）。
    - 参数：`LISTEN_MJ`（**每次**听窗口耗电，**可标定**）/ `LISTEN_INTERVAL_S`（多久听一次，
      **策略项**：走页面输入或 `ORPAH_LISTEN_INTERVAL_S`，写进标定文件只会被列“已忽略”）。
    - 口径：`listen_mw = listen_mj / listen_interval_s` 进**固定开销**；真正决定间隔的是
      `usable = 采集 − (待机 + 监听)`（页面改称「可上报」）；`net_mw` 含意不变。
    - ★ **采集连监听都供不上 → 如实报 `short_of=listen`（听不成了）+ 沉默**，
      **绝不自动拉长听间隔**（调长 = 下行变慢/发现更慢，那是产品选择）。
    - ★ 把监听接进了 `drain`/`survive_s`（漏了会让电量推进比模型说得乐观，而它会被写成
      已签的 `battery_mv`）；顺手修了「硬撑倒计时用的是不装报的乐观缺口」。
    - **实测（演示参数）**：门槛 **0.083 → 0.167 mW（约 2 倍）**；同一个 0.08 mW 原来
      “降级着慢慢撑（HS256/167s）”，加监听后**变成沉默**（连听都听不成）；
      听间隔 60→600s 则同一个 0.16 mW 从「太慢」变回「够用（ES256/150s）」——听间隔是旋钮。
    - 测试：`test_energy.py` 重写为 **73 项**，含两条锁：**关掉监听要逐位复现加监听之前的数** +
      “0.08 mW 从降级撑着变成沉默”（两边各自锁住，以后改一边会当场变红）。
    - **仍未做**：**时变取能 + 储能缓冲**（恒定 `harvest_mw` 仍不成立：光/运动取能随昼夜与
      走动波动，储能做缓冲才能回答「储能要多大撑过一夜」）—— 这是模型形状的另一半，用户可随时说。
  - ✅ **覆盖（不断线）+ 时变取能（2026-09-13 用户定口径后做；仍属“只算 + 页面呈现”）**：
    ★ 用户指出我之前把两件事混了：**取能随昼夜波动是换能器的事（物理），不是“人可以夜间停机”** ——
    ORPAH 是找人，**黄金时间含夜**、夜里更危险，**夜间/低谷是必须覆盖的时段**，“天黑就不工作”
    既不是需求也不许被画成正常循环；**断线 = 设计不足（该出警）**。→ 该做的事因此不是
    “画白天满/夜里沉默”，而是“**要多大储能/多少取能才能不断线；不够时怎么办**”。
    - 用户选项：**两种取能描述都收**（最坏缺口参数 + 实测曲线，缺省保守）与
      **覆盖不足时给“降级换覆盖”建议**（不是只预警）。
    - 实现：`energy.coverage(p, charge, gap_s=… | curve=…)` —— 缺口解析解（能撑多久 / **需要多少储能**）
      + **降级换覆盖**（HS256 + 间隔拉到 300s 上限：能多撑多久、还差多少，**绝不沉默**）
      + 曲线**分段常数积分**（最长缺口 / **最大回撤 = 所需储能** / 断线时刻 / 周期净收支 → `sustainable`）。
    - **实测（演示参数，缺口 12h）**：常态能撑 **50 min**（缺 11.2h）→ 降级后 **2.5h**（仍缺 9.5h）
      = **设计不足**；覆盖 12h 需储能 **21.6 J**，降级后 **7.2 J**（降级把储能需求降下来，这就是可执行结论）。
    - **实测（示例标定 + 24h 曲线 夜0/日1.2）**：最长缺口 6h；**撑过这一夜要 10.8 J**（降级后 3.9 J）；
      按当时电量 **1.1h 后断线**；曲线**可永续**（周期净收支 +8.6 J）；满储改 12 J → 不断线（页面转“覆盖得住”）。
    - **诚实边界**：曲线**不可永续**时“需要多少储能”不成立（只是拖时间，如实显示）；曲线路径不给
      `cover_s`（**不适用 ≠ 收支平衡**）；`extra_s` 可能为负（常态本来就不报时降级无益）。
    - 顺手修：POST `set` 里 `charge_mj` 先于 `store_mj` → “满储改大 + 同时充电”被**旧容量**无声钳掉。
    - 测试：`test_energy.py` 第 12/13 节（缺口解析解/降级口径/曲线积分/阶跃/畸形）+
      `test_energy_calib.py` 第 8 节（曲线形状时序 + 不计入 8 项计数）+ `test_appjs.check_energy_cover`。
    - **仍未做**：真机实测曲线本身（上机采，F-11）；曲线只支持“一个周期 + 周期重复”。

## 二、定位侧（track 已模拟，离真用还差）

- [~] **真数据接入而非纯模拟（拆两阶段）**：阶段一——**单台无人机多点悬停**真实 report 聚合；阶段二——**多路由器并发**（补 `router_id`/`router_pos`，改 router/server 架构）。前后端统一数据模型/API，避免模拟与真实两套逻辑。
  - 已完成（2026-09-11）：前后端统一数据模型/API（`/api/ts/query`，契约见 `API.md`）；`track.html` 新增「真实上报（IoTDB 时序）」模式，直读 IoTDB 真实 RSSI 时序 + A/n 距离换算，模拟模式保留。
  - **阶段一已完成（2026-09-11）**：补上了「这个 RSSI 是在哪个**已知坐标**测到的」这一维 ——
    `stations.py` + `/api/stations`（契约见 `API.md` §7）+ `track.html` 真实模式的「定位站位」面板。
    观测两条路、写同一张表：**时间窗**（「打点」实时生成，或悬停计划 JSON 预置 `t0/t1`；
    窗内所有 report 取 RSSI 中位数）与**手动绑定**（把最新一条 report 的 RSSI 指派给某站位）。
    ≥2 个站位有观测即调用**模拟模式同一个** `trilaterate()` → 画站位 / 距离环 / 估计点。
    站位表存 SQLite（`stations` 表），重启不丢；示例悬停计划见 `stations.example.json`。
  - **阶段二部分完成（2026-09-12，模拟器侧）**：演示数据不再是恒定 RSSI ——
    `motion.py`（闭合路线 + 匀速 + 对数距离路径损耗，确定性可复现）让被保护对象**沿路线走动**；
    `ui_server` 扮**四台路由器**（坐标 = 站位坐标），每个上报周期各写一条测量到
    `root.orpah.routers.<sid>.<sn>`（每台一条序列，互不覆盖）；设备自己 REPORT 里的 rssi
    取「当前最近那台」的测量（链路强度跟着人走）。页面用 `pos.js` 新增的**路由器序列分支**
    （绑 t 之前最近一条，时效 30 s）→ **实时页与回放页都能看到连续移动的轨迹**。
    实测：回放最后 70 s 的估计点 (9.4,21.4)→(-0.6,15.6)→(-8.6,9.8)→(-13.3,-3.5)→(1.1,-18.7)→(13.9,-18.8)，
    残差 0.0–0.8 m、GDOP 1.2；站位 1/3→3/3 随数据累加。
  - **阶段二真机侧（2026-09-12：手册 + 自检脚本已备，未上机）**：
    - 交付 = **`docs/real-hw-stage2.md`**（上机手册 + 五组验证清单）+ **`orpah/demo_hw1.py`**
      （把清单可执行化：环境/板卡状态自检、跨空口 UDP 探测、raw L2 `0x88B5` 透传探测、当接收端）。
      **两者均未经真机验证**（用户 2026-09-12 定：AI 只调研/备料，烧录与上机由用户执行），
      文档与脚本里都写明了这一点。
    - **不预设数据通路**（用户 2026-09-12 定，选项 C）：手册只列"上机后要验证什么"，
      两条候选路（A 两块 TH-RJ45 走 RJ45 透明桥 / B TX-AH+CH32V203 走 SPI）作为对照表并存，
      关键未知项**明写**：`0x88B5` 自定义 ethertype 能否透传、广播/多播透传、MTU、
      RSSI 由哪一端可得（V2.4 AP 侧 `AT+RSSI=?` = dBm）。
    - 脚本复用 `tools/ui` 的 `/api/info`+`/api/status` 读板卡状态（**不重复实现 AT 逻辑**）；
      `--peer` 探测逻辑已在本机对真实 `orpah/server.py` 跑通（发 ORPAH-REPORT → 收
      TRACKING-STATUS），即"对端不可达"与"对端可达"两条分支都验证过。
    - **仍未做（需硬件就绪后才能动，避免写没法验的码）**：真实 `router_id` 落库
      （`ui_server.py` 收到 report 时仍没传）、真实观测（真机 RSSI）写入
      `root.orpah.routers.<sid>.<sn>`、真机 A/n 标定、设备侧 ES256 签名上报。
- [~] **定位质量量化**：GDOP/PDOP、加权最小二乘（权重随距离增大而减小，σ∝距离）、误差椭圆/置信半径、滑动平均 + 卡尔曼滤波。
  - 已做（2026-09-11）：**残差 RMS**（每个站位「测量 − 估计」逐条列出 + 总体 RMS 分级）、
    **GDOP**（估计点到各站位单位向量，越小越好）、**站位布局条件数**（λmax/λmin，∞ = 完全共线）
    用于「解不出来」时区分是站位几何不行还是距离环不相交。
    为何必做：恒定 RSSI 这类退化输入会让 LS 解落在「外接圆心」，看着像个正常坐标、实际无意义。
    实测：给一组由真值点反算的 RSSI → 精确解回 `(0.0, 0.0)`、RMS 0.0、GDOP 1.22。
  - **已做（2026-09-12，加权 LS + 误差椭圆/置信半径）**：
    - **加权最小二乘**：权重 `w = 1/σ²`、`σ = max(1 m, 0.25×距离)`（RSSI 测距误差随距离增长）。
      为何必需：远端站位距离本就不可信，等权 LS 会被它拽偏 —— 实测一组「远端 +40 m 误差」：
      等权解偏离真值 **65.5 m**，加权解 **0.069 m**（远端权重被压到 1/576）。
    - **高斯-牛顿 + 步长折半线搜索**：测量不自洽（距离环不相交）时裸 GN 会振荡打满迭代，
      线搜索保证代价单调下降，并在「无步长可降」时判收敛；不收敛会**明确报出来**，不偷偷给个点。
    - **95% 置信椭圆 + 搜索半径**：协方差 `(JᵀWJ)⁻¹`（先验噪声模型）× `max(1, σ̂0²)`（后验更差才放大），
      特征值 → 半轴、特征向量 → 方位（χ²(2,0.95)=5.991）；搜索半径 = 沿最长轴的 95% 覆盖半径。
      **为何不用 σ̂0²(JᵀWJ)⁻¹**：恒定 RSSI 这类「自洽但错误」的输入残差≈0 → σ̂0→0 →
      会给出「零不确定度」的假自信（本页一直在防的那类坑）。
    - 页面：真实模式新增「加权解 / 95% 置信 / 搜索半径」三行 + 每站位距离带 ±σ；
      画布与地图都画椭圆（黄虚线）；`track.html` 模拟模式仍用原线性解（本项只动真实模式）。
    - 验收：由真值点反算 RSSI → 绑定 → WLS 复原到 **(5.0, 5.0)**（真值 5,5）、残差 0.1 m、
      椭圆 19.8×12.3 m、方向 67°；精确距离输入复原误差 2.7e-15；共线站位→加权法方程奇异（回退线性解+提示）。
  - **已做（2026-09-12，时序平滑 = 恒速卡尔曼）**：
    - **为什么是卡尔曼、不是滑动平均**（先回答了「与搜索半径的关系」才动手）：移动目标上 N 点平均
      的滞后 ≈ (N/2)×采样周期；找人场景里「滞后」就是「指错位置」。合成场景（半径 12 m 圆、
      1.2 m/s、2 s 采样、RSSI ±2 dBm）实测：原始 RMS 4.5 m → **卡尔曼 3.5 m / 5 点平均 3.4 m**
      （降噪量接近），但**滞后 卡尔曼 0.4 m vs 5 点平均 2.7 m**。且只有卡尔曼能给出**滤波后协方差**。
    - **搜索半径用滤波后协方差 P（含过程噪声 Q + 测量噪声 R），不用测量协方差**：
      否则会把「输出更平滑」误当成「更准」，给出**过小**的搜索半径（假自信 —— 本页反复防的坑）。
      实测平滑后半径 27.3 m → 21.9 m（σ 7.5 m → 6.3 m），**95% 椭圆对真值的覆盖率 100%**（合成场景，
      100 帧）→ 半径可信、没有过度自信。
    - **实现**：`pos.js` 新增 `kalmanTrack(frames)`（4 状态 CV：x,y,vx,vy；松耦合位置量测；
      长时间缺口 reset；输出 `{t,x,y,vx,vy,cov,reset}`）+ `pathJitter()`。
      **协方差更新用 Joseph 形式** `P=(I−KH)Pp(I−KH)ᵀ+KRKᵀ`：更短的 `P=(I−KH)Pp` 在浮点下会丢掉
      对称正定，跑几十帧后创新协方差近奇异 → 增益暴冲 → 状态发散（**实测**：真实数据第 84 帧
      从 −11 m 跳到 12000 m，改用 Joseph 后全窗 793 帧全部有限）。
    - **工程决策：坚持算法单一源** —— A/n、观测取值（bind > 窗口中位数 > 路由器序列）、WLS、滤波
      全在 `pos.js`，`track.html` 与 `replay.html` 共用；页面只画两条线（红=原始、绿=平滑），不另写算法。
    - **为让平滑可验证，先补了测量噪声**（`motion.py` 的 `NOISE_DB=2.0`，SHA-256 确定性噪声、逐站位不同）：
      无噪声数据的定位结果本来就是真值，平滑/椭圆/残差都失去意义（RMS 恒 0.12 m）。
    - **诚实的边界**：真实演示数据噪声小（±2 dBm），而 WLS 的噪声模型 `σ=max(1, 0.25×距离)` 偏保守
      （比实际大 ~2.4 倍）→ 滤波更信测量 → **真实数据上平滑幅度有限**（抖动 3.5→3.1 m，−12%）。
      若真机 RSSI 误差标定得更准，可收紧该模型让滤波发挥更大作用（属阶段二标定工作）。
  - **未做**：无（时序平滑完成）。
- [~] **单/双/三点策略自动化**：自动判定可用测点数 + 几何共线检测，不满足自动提示补站位。
  - 已做（2026-09-11）：可用测点数自动统计（真实模式「参与定位：n/t 个站位有观测」），
    不足 2 个时直接提示、不出估计点。
  - **已做（2026-09-12）**：置信半径 —— 95% 椭圆 + 「搜索半径」（沿最长轴的 95% 覆盖半径），见上一条。
  - 未做：自动提示补站位的策略建议（当前只到「报告几何不行」）。
  - **已做（2026-09-13，补站位建议 —— 把“几何不行”变成一句可执行的话）**：
    `pos.js` 新增纯函数 `suggestStation(obs, est)`；`track.html` 站位卡新增「建议补站位」行 + 画布/地图上画建议点。
    - **判据分两种（关键）**：有估计点 → 用**该点的 GDOP**；没有估计点 → 用**布局条件数**。
      共线这种“根本解不出来、压根没有估计点”的场景也能给建议（条件数只用站位坐标）。
      返回里 `scoreBy` 如实标本次用哪个判据，不藏着。
    - **候选网格 + 隔离门槛**：站位包围盒按**布局尺度（两轴较大跨度）**各向外扩 50%，13×13 网格；
      与已有站位距离 < （站位间最大跨度 × 0.25）的候选剔除（两台贴在一起等于一台）。取 score 最小者。
    - **自己踩的坑（实测）**：① 外扩起初**逐轴**用该轴跨度 → 共线布局 y 跨度为 0 → 候选被压成
      “离直线 0.5 m”，建议等于没说（实测输出 `y=-0.5`）→ 改成按布局尺度外扩后建议在 (30, -22.5)。
      ② 取整辅助函数把 `null` 变成 `0`（`isFinite(null)` 为真）→ gdop 无值时会显示成 **0 = 完美几何**；
      已改为 “null 保持 null”（本仓“无值不给 0”的约定）。
      ③ **`const adv` 写在 `drawLoc(…, adv)` 之后 → 暂时性死区让 `renderStations` 整个抛 ReferenceError**
      （页面全是 “-”，零控制台报错堆栈误导）—— 实测浏览器抓到，已把计算提到使用之前。
    - **四种情形分开说，不合并成模糊话**：有坐标→「再放一台：(x, y) · 距最近站位 d m · 条件数 ∞ → 2.83」；
      `too_few`（只有 1 个站位有观测）→「先让 ≥2 个站位有观测」且**不给坐标（不编）**；
      null + 有估计点 →「无需补（几何够好）」；null + 无估计点 →「不是摆位问题（几何不弱，距离环不相交）」
      —— 后两种情况都返回 null，**由页面按有没有估计点区分**（避免把机制塞进纯函数里）。
    - 测试：`test_posjs.py` 新增 17 条（GDOP 单一实现/共线建议离开直线/有估计点改用 GDOP 且真的下降/
      好几何返回 null/两点秩退化/只有一台不给坐标/无解但几何不弱不给建议/确定性/0.1m 取整/null 不为 0）。
    - 实测（浏览器，真实模式三台站位）：好三角 → 「无需补」；改成三台共线 → 「再放一台：(30, -22.5) ·
      条件数 ∞ → 2.83」+ 画布琥珀虚线方块（期望坐标处 17×17 像素内 56 个琥珀像素）；中英双语均正确；
      改回后建议自动消失。
- [~] **离线地图与野外包**：PMTiles/区域离线包切换、**经纬度基点导入（2026-09-13 完成：导入/导出 + 校验 + 本机记忆）**、**轨迹导出 GPX/GeoJSON（2026-09-12 完成）**、**无底图回落（2026-09-12 完成）**。
  - **已做（2026-09-12，轨迹导出）**：回放页新增「导出轨迹」（原始 / 平滑）× GPX / GeoJSON。
    要点：① 导出**整窗**轨迹（不是只导已回放部分——导出是拿来用的成果，缺时间段不应被截掉）；
    ② **缺口断开成多段**（相邻帧间隔 > `ROUTER_MAX_AGE_MS` = 观测时效；平滑侧再按滤波器 `reset` 断开）——
    跨缺口的直线不是真走出来的路，GPX 用多个 `<trkseg>`、GeoJSON 一段一个 `Feature`（LineString），
    实测：造 30 s 缺口 → 段长 [3,2]；仅 `reset` 无缺口 → 平滑 [3,2]、原始 [5]（两条规则各自生效）；
    ③ 经纬度走 `map.js` 的 `toLatLng`（**与地图视图同一个换算**，基点 + 米/度）→ 导出落点与地图上看到的一致
    （实测首点 lat/lon 与 `toLatLng` 逐位相同、GeoJSON 坐标是 `[lng, lat]`）；
    ④ GPX 1.1 带 `<time>`（可被 QGIS/Google Earth 直接读，实测 DOMParser 无错、795 点时间严格递增）；
    ⑤ 无数据的窗口给明确提示「本窗没有可导出的定位轨迹」，不产出空文件；
    ⑥ 文件名 `orpah-<SN>-<raw|smoothed>-<起>-<止>.gpx|.geojson`（SN 里非 ASCII 字符替换为 `_`，
    因为 SN 允许中文）。**纯前端**，无新后端接口。
  - **已做（2026-09-12，B = 无底图回落）**：瓦片连不上时地图**不留白** —— 自动切到**本地自绘网格底图**
    + 比例尺 + 明确提示（含「重试底图」），轨迹 / 距离环 / 定位估计照常画。
    要点：① 判据 = **连续 4 张瓦片失败且成功数为 0**（个别瓦片 404 不误判）；
    ② 自定义源没填 URL 时直接判定离线；③ 网格是 canvas 现画的，**不含任何第三方数据** → 无许可问题；
    ④ 恢复网络后点「重试底图」即回到真底图（实测：断图 → 24 块网格 + 提示 + 14 条 overlay 照画；
    重试（仍断）→ 再次回落；恢复后重试 → 24 块真瓦片、提示消失）；
    ⑤ 回落逻辑在 `ui/static/map.js`（`addBaseLayer()`，两页共用，单一源）。
    **演示离线效果不用拔网线**：把「自定义瓦片 URL」填成不可达地址即可。
  - **已做（2026-09-13，野外包切换 UI = 本地 XYZ 瓦片目录；用户选“最小方案”）**：
    现场没网时的底图，不引第三方库、不需要 Range。
    · **服务端** `maps.py`：`list_packs()` 列包（只算有瓦片的目录；数到 `COUNT_CAP` 就停并标 `truncated`）
      + `resolve_tile()`（**路径穿越的唯一防线**：只认 `<包名>/<z>/<x>/<y>.<png|jpg|jpeg|webp>`，
      包名不得含 `/`、`\\`、`..`、不以 `.` 开头，层级必须十进制且在 zoom 范围内）→
      `ui_server.py` 的 `/api/maps`（列表）与 `/maps/…`（**只整文件发**）。
    · **前端** `map.js`：瓦片源列表新增 `野外包（本地）`（放最后、默认不选）+ `#packSel`/`#packNote`/`重新扫描`；
      URL = `/maps/<包名>/{z}/{x}/{y}.png`，选择记在 `localStorage`（现场不用每次重选）。
    · **五种状态分开报**（现场最容易看错）：有包 / 没有包 / 取不到列表 / **选中的包消失了** /
      没选到包（空 URL → 直接离线回落）；包不存在或瓦片 404 → 按 B 方案回落网格底图，**不留白**。
    · **包不入库**：`.gitignore` 加 `ui/static/maps/`（运行时素材、可能几十 MB）；
      `test_mapjs.py` 把这条当**守卫**锁（连同前端模板 ↔ 服务端解析器**同形状**：改一边不改另一边 = 一律 404）。
    · 合规：必须**自建/自托管**（OSM 官方瓦片政策禁止离线，见 §七）—— 条款文案 `tile_note_pack` + 口径守卫。
    · 验收：`test_mapjs.py`（node 73 项 + 页面/口径/**野外包**守卫）；真机实测（造 2×2 小包）：
      列表能列出来/选中后地图真的拉 `/maps/...` 瓦片、包删掉后回落网格底图 + 提示、
      重新扫描不需要重启服务端。
  - **未做（A = PMTiles 区域离线包）**：用户 2026-09-12 定「不下包」，2026-09-13 又选「野外包用 XYZ 目录」
    → 本项**只留做法**，将来要做时照此办：
    - 合规前提：**不得**基于 `tile.openstreetmap.org`（政策明文禁止离线）；用 **Protomaps 公共 build**
      （数据源 = OSM，ODbL，允许自托管/离线，需署名）抽一小片区域：
      `pmtiles extract https://build.protomaps.com/<日期>.pmtiles --bbox=<西,南,东,北> region.pmtiles`
      （`pmtiles` CLI 走 HTTP Range，只下需要的部分，城市小区域通常几 MB）。
    - 前端：`pmtiles.js` + Leaflet 的 PMTiles 图层（栅格）或 `protomaps-leaflet`（矢量）。
    - **服务端必须先支持 HTTP Range**（`ui_server.py` 静态处理目前不支持 → 要补 `Range` 响应头/206），
      否则 PMTiles 拉不动。
    - 产物放哪（用户 2026-09-12 定）：**不入库**（`.gitignore` + 文档写清生成命令与用途），
      避免仓库被大文件撑大；引用它的代码要能在缺文件时**明确提示**而不是静默空白。
    - **授权约束（2026-09-11 核实，必须遵守）**：`tile.openstreetmap.org` 的 Tile Usage Policy
      **明令禁止离线**（"Offline use is not permitted on tile.openstreetmap.org"），也禁止预取/批量下载/
      建瓦片包；且无 SLA、可无预警封禁，policy 原文提醒「商业服务可能随时失去访问」。
      故本项**不得**基于 OSM 官方瓦片，必须自建瓦片（switch2osm，PMTiles 自建即合规）
      或改用明确允许离线的供应商。
  - **已做（2026-09-13，基点导入/导出 + 记忆）**：基点 = 把本地格网锚到经纬度的**那一个标定输入**，
    以前只能手抄两个输入框（7 位小数极易抄错，而**基点错 = 整条轨迹平移**）。现在：
    · **导入/导出同一个 JSON 格式**（`orpah_base:1` + `lat/lng` + 可选 `name/note/when`）→ 导出可原样再导回来
      （有往返用例）；解析还认 `latitude/longitude`、`lat0/lng0`、`baseLat/baseLng`、
      `origin|base|center|point` 子对象、以及 **GeoJSON Point/Feature/FeatureCollection**
      （注意 GeoJSON 顺序是 **[经度, 纬度]** —— 反了会落到地球另一边，已单写一条用例）。
    · **校验与错误码可见**：非法 JSON / 认不出字段 / NaN·空串·∞ / 超出 ±90·±180 各自给码，
      页面译成可读文案（带 detail 与实际值）；**绝不静默回落成默认值**（那会让人以为导入成功了）。
    · **单一源**：`BASE_DEFAULT` 只在 `map.js`（两页 `value=` 只是离线兜底，`test_mapjs.py` 守卫两者同值）；
      解析/校验/换算/存储/按钮接线全在 `map.js`，两页只放元素。
    · **存在本机浏览器**（`localStorage`，不上传服务端；清了/换机器就要再导一次）、开页自动套用、
      **来源三态**（default / manual / imported）显示在状态行 —— 免得把演示默认值当成现场值。
    · **口径如实**（写进页面提示与守卫）：只接受 **WGS84 十进制度**、**不做坐标系转换**
      （GCJ-02/BD-09 会被原样当 WGS84 → 可能偏几百米）；换算是**等距圆柱近似**（米/度按基点纬度取 cos）
      → 基点要取在场地附近（几公里内）；纬度方向是纯平移，经度方向另有一个与 x 有关的极小比例项。
    · 验收：`test_mapjs.py`（**54 项** node 跑 `map.js` 原文 + 页面守卫 8 项 + 口径守卫 6 项）；
      真机实测：导入 → 基点与**已开着的地图中心**同步变、刷新后仍在、回放页共享同一份、
      坏文件给原因且**不改**当前基点、复位回演示值并清掉本机那份。
  - **已做**：野外包切换 UI（= 本地 XYZ 瓦片目录，见上）。**仍未做**：PMTiles 区域包（A）。

## 三、案件闭环 + 安全事件与运维

- [x] **走失案件闭环（以人为单位，非单 SN）**：某人走失 → 标记（其名下所有设备进入走失态）→ 路由器发现任一台即视为发现该人 → 找回/撤销 → 结案。case 状态机 + 页面（`cases.py` + `case.html`，2026-09-11 完成）。多设备定位聚合并入「真实 report 聚合」。
- [x] **审计日志**（2026-09-11 完成）：走失标记、撤销、发现、验签（含被拒）、发布等业务事件持久化到 IoTDB `root.orpah.events`，`/api/ts/events` 可回读、`index.html` 有「事件历史」区，**重启后仍在**。
  - **谁**：事件带 `actor` 字段——人为操作（立案/结案）由页面随 POST 带上（共享组件 `ui_i18n.js` 的 `bootActorBox()`，值存 localStorage），自动事件记 `system`；事件历史只给人为操作显示操作者标签。
  - **保留期限**：`ORPAH_EVENT_RETENTION_DAYS`（默认 30 天，0=不清理），启动连上库后按期限清理一次；`/api/ts/events` 回 `retention_days`/`purged_upto`，页面显示「保留 30 天」。
  - **可筛选**：验签被拒的单写 `id_reject`（原来混在 `id_report` 的 detail 里，无法按类型筛）。
  - **未覆盖**（后续）：`/api/registry` 增删改、`/api/stations` 增删改/打点/绑定 这些写操作不产生审计事件；也无「谁」的鉴权/角色（仅自填标识）。
- [x] **告警与通知**（2026-09-11 完成，仅「页面红点」部分）：`alerts.py` 无状态规则引擎（长未上报 / 走失超时 / 签名失败率）→ `GET /api/alerts`，`index.html` 页头徽标 + 告警卡片，3s 轮询；条件消失则告警自动消失。
  - **阈值可配**：默认 30s / 180s / 5 条 / 0.5 是**演示压缩时间**（客户端 2s 一包），
    全部可用环境变量覆盖（与保留期限同机制，见 `API.md` §8）：
    `ORPAH_ALERT_NO_REPORT_SEC` / `ORPAH_ALERT_CASE_OVERTIME_SEC` /
    `ORPAH_ALERT_SIG_WINDOW` / `ORPAH_ALERT_SIG_FAIL_RATIO`。
    演示时若嫌「立案 3 分钟就报警」，设 `ORPAH_ALERT_CASE_OVERTIME_SEC=600` 即可（不用改代码）。
    ⚠ **默认值分两类**：`no_report` 30s / `case_overtime` 180s / `sig_window` 5 / `sig_fail_ratio` 0.5
    是**演示压缩时间**；`case_handled_overtime` 的 **86400（24h）是真实时长**（演示里不会自然发生，
    要看效果需压小）—— 别把两类混着读。
  - **✅ 复核修正（2026-09-12，code review 提出）**：`no_report` 原来只盯 `STATUS_ACTIVE`，
    而**立案会把设备转成 `STATUS_LOST`**（`cases.mark` → `registry.set_statuses(..., LOST)`）
    → 走失者的追踪器一旦掉线（没电/出范围，往往正是「找不到人」的原因）**反而不告警**，方向反了。
    现改为工作态 = **启用 或 走失**（停用/报废仍不盯；「从未上报」的守护也保留，避免开机一片红）。
    测试：`test_alerts.py` 覆盖 启用/走失 都报、停用/报废/未上报不报。
  - **✅ 已做（2026-09-12，A 方案：加处置态）**：
    - **`cases` 加 `handler` + `handled_at`（与 `status` 正交的一维，不是新状态）** ——
      理由：「谁在办」与「走到哪一步」是两件事（已发现但没人管、已接手但还没找到都合法），
      做成状态就要改 `active_case`/`open_cases` 等所有判定。页面上的「处置中」= 还开着 **且**
      有接手人，是**派生显示**（`to_dict.handling`）。
    - **接手人只用自由文本**（复用审计 `actor`，`case.html` 的操作者输入框）；**不建 operators 表、
      不做登录**（否则滑向 L2/L3 与 §〇 范围原则）。改派＝再点一次；取消接手也可
      （`handler=""`），两种都写 `case` 事件（`assign`，detail=接手人），历史不丢。
    - **规则改为「超时 且 无人接手」才告警**：`alerts.evaluate` 里 `case_overtime` 跳过
      `c.handler` 非空的案件 → **红点不再恒亮**（实测：接手后 `case_overtime` 从告警列表消失，
      取消接手后回来）；告警文案同步改为「…仍未被发现，且无人接手」。
    - UI：案件页状态列显示「处置中」徽标 + 「未接手 / 接手人 X」；操作列加「标记已接手 / 取消接手」
      （改派时两个按钮都在）；工具栏加「只看未接手」筛选；事件行显示「接手（接手人 X）」/
      「取消接手（未接手）」。
    - 审计：`POST /api/cases {action:"assign"}` 写 IoTDB `case_assign` 事件（`actor` = 接手人，
      `detail = <case_id>:<handler>`）；`index.html` 事件历史可用 `evt_type_case_assign` 显示中文。
    - **老库迁移**：`cases` 表加了两个列，`CREATE TABLE IF NOT EXISTS` 不会给老表补列 →
      `_init_db` 用 `PRAGMA table_info` 检查并 `ALTER TABLE ADD COLUMN`（否则老 `orpah.db`
      下一次写库就报 `no such column: handler`）。实测：手造一个没有这两列的旧库 → 自动补列、
      旧案件可读可写。
    - 测试：`test_alerts.py` 加 5 项（已接手的超时案件不告警 / 接手后消失 / 取消后回来 /
      真实对象上真的调 `cases.assign` 再评估）。**注意**：该文件的假 `Case` 对象必须带 `handler`
      属性（缺属性会让测试直接崩，多给属性则会掩盖真实 AttributeError —— 两种都踩过）。
    - ⚠ **B 方案已完成（2026-09-12）**：`case_handled_overtime` —— 已接手的案件，
      从**接手时刻**（`handled_at`）起再超 `ORPAH_ALERT_CASE_HANDLED_SEC`（默认 24h）仍未发现 → 再提醒。
      级别 **warn**（“有人在办、拖太久”的提醒，不盖过“没人接”的 crit）；`since` = 接手时刻。
      为什么需要：A 方案让「已接手」变成**永不再报**，案子认领后搁置（人没找着、也没人再管）就完全静默。
      实测（在 simulator 的**临时副本**上跑，不动演示库）：新库播种的 open 案件 assign 后，
      带 `ORPAH_ALERT_CASE_HANDLED_SEC=5` 的实例在 7s 时给出 `case_handled_overtime`
      （`level=warn`、`handler=验证-张警官`、`gap=35s`、`since=handled_at`、`counts={crit:0,warn:1}`）；
      取消接手后该告警消失。A 的 `case_overtime` 与 B 的互斥/排序由 `test_alerts.py` 覆盖。
    - ⚠ **C 方案已完成（2026-09-12）**：**按持续时长分级/升级** —— 让「拖得越久越严重」能表达出来。
      只给**「沉默得越来越久」型**的两条加升级（严重度随时间单调增长）：
      `no_report` `>30s` warn → `>300s`（`ORPAH_ALERT_NO_REPORT_CRIT_SEC`）crit；
      `case_handled_overtime` `>24h` warn → `>48h`（`ORPAH_ALERT_CASE_HANDLED_CRIT_SEC`）crit。
      **不给** `case_overtime`（无人接手）与 `sig_fail_rate`（验签被拒）分级：它们是**定性**问题，
      一发生就该 crit；加一段 warn 会把 A 刚解决的「刚超时先不报红」又拿回来。
      升级阈值设得 ≤ 起步阈值 → 直接 crit（等于关掉分级，保留旧行为）。
      测试：`test_alerts.py` 加 11 项（默认阈值、升级点两侧与边界、分级不改变"盯谁"、
      B+C 合用、两条不分级的仍是恒 crit、crit 排在 warn 之前、env 覆盖与"关闭分级"）。
    - ⚠ **仍未做**：无（A/B/C 均已完成）。
  - **✅ 已做（2026-09-13，#1 告警通知渠道）**：以前告警只能被“正在看页面的人”看到 —— 没人在看等于没有告警。
    - **出站 Webhook（`notify.py`）**：`ORPAH_NOTIFY_URL` 指向接收端（空 = 关闭），HTTP POST JSON，
      内容只给机器可读字段（`source/event/ts/key/alert`，`alert.msg` 是 **i18n 键**）—— 与页面同一约定。
      **边沿触发**（这是这个模块存在的理由）：只推「新 `key`」与「同一 `key` 级别升高（warn→crit）」，
      同级告警持续期间**一条都不重复推**（不差分就是每 3 秒刷一遍接收端）。
      **失败可见**：计数 + `last_error` + 审计 `notify` 事件（含 `tries`）；**有界重试**（见下一条）。
      `seen`只在内存 → 重启后活跃告警会被重推一遍（如实写清）。
    - **后台巡视线程**（`ui_server._alert_watch`，每 `ORPAH_NOTIFY_SEC`=3s）：
      **没人开页面也能通知到**；与 `/api/alerts` 共用同一个视图函数（`alert_view()`）——
      页面看到的与通知推的必须是同一份判定，否则会出现“页面红点亮了但没通知”。
    - **页面弹窗（SSE 边沿事件）**：服务端只在投递发生的那一刻 `_emit_alert`，
      所以弹窗不会每 3 秒弹一遍同一个告警；弹窗说的是“**通知发出去了没有**”
      （`已推送 / 推送失败：… / 未配置地址`），与上方红点（当前状态）是两件事。
    - **页面显示当前阈值**：`/api/alerts.thresholds`（单一源 = `alerts.thresholds()`，含 env 覆盖）——
      16 条阈值名/值/单位全由服务端下发，页面不写死数值也不写死“该显示哪几条”。
    - **通知卡**：Webhook 地址 + 最低推送级别（warn/crit，运行时改不重启）+ 「测试发送」+
      已推/失败/低于级别未推/正在跟踪/最近错误 + 最近投递表（事件/告警/级别/结果/说明）。
    - 验收：`test_notify.py` **42 项**（边沿触发/升级再推/降级不推/消警可选/min_level/
      失败可见/有界重试与放弃/队列上限与丢最旧/每 tick 上限/换地址丢队列/测试发送不计入计数/运行时配）；
      真链路联调：真起一个假接收端（8899）→ 启动时推出的 `case_overtime` 只 1 条、等 8 s 不重复；
      3 条 `bad_check` 连发 → `badcheck_streak` warn + `sig_fail_rate` crit 各推一次；
      地址改成死端口 → 「测试发送失败：URLError…」与最近错误均可见。
  - **✅ 已做（2026-09-13，#5 通知健壮性与 UX）**：
    - **有界重试 + 退避（默认 1s/5s/30s，首次失败后再试 2 次）**：失败不再“算一下数就完”；
      **试完仍失败才计「放弃投递」**。三个计数分开：`sent`（最终送达，含重试成功）/`failed`（放弃）/
      `retried`（多试了几次）—— 合成一个数就说不清“到底送没送到”。
      **语义是至少一次**（POST 超时的那条对方可能已收到 → 接收端要按 `key` 去重），不得写成 exactly-once。
      **每次 tick 最多试 3 条**（`ORPAH_NOTIFY_PER_STEP`）—— 否则端点挂掉时积压×3s 超时会把
      **后台巡视线程**卡住，连告警评估都停了（比丢通知更糟）。队列上限 20，满则丢最旧并计 `dropped`。
      **仍不持久化**（队列在内存）：重启丢掉未送出的重试 —— 如实写在页面上，不假装。
    - **弹窗可“点进去看”**：`ALERT_LINK` = kind → 目标页（可带 `#锚点` / `?sn={sn}`）的**唯一映射**；
      键盘可达（`tabIndex` + Enter/Space）。**`test_appjs.py` 从 `alerts.py` 现抽 kind 逐个核** ——
      漏一个 kind 就是那条告警弹出来**点了没反应**（静默失效）；多余/拼错的 key 也报。
      落点用 `?sn=` 时页面必须认这个参数：`track.html` 现在支持 `?sn=`（并自动切「真实上报」模式），
      SN 不在清册时**补一个选项**而不是静默落到别的设备（“看错设备”比报错难查）。
    - **声音提醒（默认关，勾选开启）**：WebAudio 现场合成（不带音频文件）；crit 两声 / warn 一声 /
      消警一声低音。**浏览器自动播放策略**下取不到 `AudioContext` 就**静默不响**（不假装响过）；
      文案如实：它只是**本机提示音**，不等于“通知已送达”（送达看 Webhook 那一列）。
      默认关由 `test_appjs.py` 锁（HTML 不得带 `checked` + `localStorage` 严格 `=== "1"`）。
  - **✅ 已做（2026-09-13，第二批两条规则 —— 此前写着“需要历史序列，本模块暂不做”）**：
    - **`rssi_jump`**（RSSI 突变，warn）：设备**自报**的链路强度相邻样本跳变超阈 → **仅线索**。
      **阈值按实测零假设分布定**（不拍脑袋）：诚实序列 = 演示行走模型里设备自报的 rssi
      （±2 dBm 确定性噪声）按 1/2/5/10 s 间隔采样 2 h，量相邻样本 |Δ|：
      中位 1.0/1.0/2.0/4.0、**max 5.0/7.0/9.0/12.0**。
      **跳变随采样间隔线性增长**（人在走）→ 固定 dB 阈值会在把上报间隔调大时误报，
      故只在 **0.5~15 s** 的相邻样本间比、阈值取 **25 dB**（诚实上界 12 dB 的 2 倍余量），
      间隔 >15 s 不比（分不清“突变”与“走了很远”，宁可不报）。
    - **`badcheck_streak`**（SN 校验位连续失败，warn）：从**最新一条往回**数 `bad_check`，
      **中间夹了任何其它结果（通过 / 别的拒绝码）就停** —— 「连续」是字面意思；默认 3 条起报。
    - 两条都带「**仅线索、需人工复核**」文案（术语硬规则：偏差/不一致不得归因）；
      实测发现的连带现象：5 条连败会**同时**触发 `sig_fail_rate`（两条规则从不同角度描述同一串报文，
      都出是对的），已写进 `test_alerts.py` 的注释。
    - 回归锁：诚实序列 **12956 对样本**（四种间隔 × 2 h，逐对样本评估）**零误报**。
  - **未做**：邮件（需 SMTP 凭据，demo 不做，如实标注）、Webhook **持久化队列**
    （重试队列在内存里，重启丢未送出的；重试/退避/弹窗跳转/声音提醒已做，见上）。
- [ ] **案件经办 / 分配（"谁在办"）**（2026-09-11 用户提出，本次只记录、不实现）
  - **与上一条「处置态」的区别（别混）**：处置态里的 `handler` 是 **L1 级**（自由文本"谁点的"），
    只回答"这单有人接手了吗"；本条的 L2 是**分配/改派的对象**、能回答"哪几单没人办""派给谁了"，
    需要第二类人（经办人）落库。**两者不冲突**，但 L2 的"动手前必须先定两件事"依然成立。
  - **现状盘**：**立案已有**（`case_mark`，「标记走失 / 立案」）；**分配/经办没有** ——
    案件无归属人、无"派给谁"动作。仅有三个**自由文本**信息位
    （`police` 警方联系人 / `police_case_no` 案件编号 / `police_station` 受理单位），
    **信息位留了、实体没建**。
  - **`actor` 现状**（别误读）：只是审计里的**自由文本标签**（`case.html` 的「操作者」输入框
    → localStorage → `root.orpah.events.actor`），**无身份/无校验/不落在案件上**，调用点仅
    立案与结案两处。之前演示里出现的「张警官」是**测试时手输的值**，代码与种子数据中无此角色
    （全库仅 UI 占位文字「如 XX派出所 王警官」）。故 `actor` ≠ 经办人。
  - **三个量级**（成本差很大）：
    | 层 | 内容 | 成本 |
    |---|---|---|
    | L1 审计标签 | 自由文本"谁点了按钮" | ✅ 已有 |
    | L2 经办人（= 分配 + 经办） | 案件有归属人、可分配/转派；能查"谁在办""哪几单没人办" | 中（见下） |
    | L3 组织/角色/权限 | 机构（派出所）、角色（接警/办案/协查）、权限边界、多租户隔离 | 大（= 半个业务系统，见 §四） |
  - **若做，推荐先只做 L2 子集**：`cases` 加 `handler` + `assigned_at`，加 `assign` 动作（含转派），
    记审计（复用 `actor`：谁把案子派给谁），`case.html` 展示 + 筛选。**不做**登录/密码。
    好处：立刻能回答"这单谁在办 / 哪几单没人办"，并把 §三告警那条的缺陷一并解决
    （A 方案的「已接手/处置中」就是 L2 的前半）。
  - ⚠ **动手前必须先定两件事**（否则会做歪）：
    1. **两类人不能同表**：`registry.persons` 是**走失者台账**（被寻找对象）；经办人是
       **系统中的人**（操作者）。加 L2 会出现第二类人，必须另存（如 `operators` 表），
       绝不能塞进 `persons`。
    2. **命名歧义**：现在的"立案"是 **ORPAH 侧登记**（把该人名下设备标走失态 + 下发 LOST-TABLE），
       **不是公安立案**。两者含义不同，混着叫会让人误以为本 demo 是公安办案系统。
  - **边界（重要）**：ORPAH 是**技术搜寻手段**，不是公安办案系统 —— 真实的"立案/分配/经办"
    属警方系统职责；ORPAH 侧真正需要的只是"这单谁在跟、找谁对接"。故 **L3 很可能超出本项目边界**，
    除非要把 demo 当"运营平台"演示。做 L2 前建议先确认这两类人是否真的存在（demo 里目前没有登录者）。
- [~] **回放**：按时间段回放报文流 + 定位轨迹 + 安全事件。
  - **主体已做（2026-09-12，P1）**：`replay.html` + `GET /api/replay`（契约见 `API.md` §10）。
    数据源 = **IoTDB 时间窗**（`WHERE time >= x AND time <= y`，时间过滤走原生索引，重启不丢），
    一次取回「该设备在本窗的上报点（升序）」+「本窗的业务事件（升序）」；
    窗口上限 12 小时、点数上限 20000（超了置 `truncated`），页面提供起止时刻 + 10/30/60/180 分钟快捷窗。
  - **播放**：播放/暂停 + 倍速（1×…1200×，语义 = 回放秒/真实秒）+ 进度条拖动定位；
    画布画站位、距离环、逐帧估计点（红叉）、95% 椭圆、已回放轨迹；
    面板给出「参与定位 / 估计 / 残差 RMS / GDOP / 搜索半径 / 当时 RSSI / 最近事件」。
  - **事件时间线**：按时间升序列出本窗全部事件（含 `id_reject`/`key_*` 安全事件，可「只看安全事件」筛选），
    光标之后的事件置灰（`future`）、当前条高亮并自动滚到视野内。
  - **不重写算法**：抽出了共享定位内核 `ui/static/pos.js`（`trilaterate`/`wlsLocate`/`ellipseOf`/
    `obsOfStation`/`qualityOf`/`layoutCond`…），`track.html` 与 `replay.html` 用**同一份**实现。
    验收：实时页与回放页在同一时刻/同一站位表下，估计、RMS、椭圆半轴、搜索半径逐位一致。
  - **不看未来**：`obsOfStation` 内置 `p.t <= t` —— 回放某时刻只看该时刻**之前**的样本
    （否则会提前知道后面的测量）。
  - **地图视图（2026-09-12 完成）**：回放页可切「画布（本地坐标）⇄ 地图（真实地理）」：
    站位、距离环（半径即测距）、该时刻定位估计、95% 置信椭圆、已回放轨迹都画在真实底图上；
    轨迹**跨无观测缺口会断开**（拆成多段折线，不会画成一段直路）；开地图时按「站位 + 轨迹」
    `fitBounds` **自动框住场景**（演示场景只有 ~60 m，默认 zoom 下几乎看不见）。
    底图源/条款说明/本地坐标↔经纬度换算抽到 **`ui/static/map.js` 单一源**（与实时页共用）——
    那是**合规相关**的东西（各源署名/离线限制不同），拄两份迟早漂移。
    ⚠ 场景尺度提醒：演示路线在 `motion.py` 里只有 ~60 m（四台路由器围的一圈），
    地图上显得很紧；要看更大的场景改 `motion.WAYPOINTS`（路由器坐标 = 站位坐标）。
  - **轨迹导出 GPX / GeoJSON（2026-09-12 完成）**：回放页「导出轨迹」= 原始 / 平滑 × GPX / GeoJSON，
    导出**整窗**轨迹、**缺口断开成多段**（GPX 多 `<trkseg>` / GeoJSON 一段一个 LineString Feature）、
    经纬度走 `map.js` 同一个 `toLatLng`（与地图视图落点一致）。详见 §二「离线地图与野外包」条。
  - **报文流时间轴（2026-09-13 完成，本页 §3）**：把「无线链路上收到了哪几帧」单独做成可浏览的
    时间轴（与 §2 分工：§2 = 业务/安全事件，§3 = 原始上报帧）。**数据来源** = 回放接口已有的
    `points`（`[{t,rssi,seq,router_id}]`，升序）→ **没有新后端接口**。
    - **判定全走 `pos.js` 的 `frameGaps()`（单一实现 + 10 条单测）**，页面只负责画：
      `d>1` → 缺口 `d-1`；`d===0` → 重复；`d<0` → **回退**；三者**分开计**。
    - ⚠ **纠正本清单原来的一句话**：「`points` 带 `seq`/`router_id` 已在数据里」**只说对一半** ——
      `seq` 有值，**`router_id` 是空串**：`tsdb.write_report` 的该参数从没被传过
      （`ui_server._on_report` 没传；真机阶段才填，见 §二 真数据接入 + `docs/real-hw-stage2.md`）。
      页面因此**如实显示 `（空）`**（复用既有 `tk_router_none`），真机侧填上后这一列自动亮。
    - **为什么值得做（排障价值，给阶段二用）**：① **序号缺口 = 上行丢包证据**；
      ② **帧间隔中位/P95**（实测演示数据 2.0s / 3.0s）能看出上报周期抖动；
      ③ 点某一行 → 光标跳到该帧（与拖进度条同一条路径，不另建一套跳转逻辑）；
      ④ 与事件时间线同样的「灰=未来 / 高亮=当前 / 播放时自动跟随滚动」。
    - **自己踩的坑 1（测试抓到的）**：`Number(null) === 0` → 「没有 seq」被当成 seq=0
      → 整段数据被算成“一路回退”。已加显式 `hasSeq()` 守卫（`null`/`undefined`/`""` 都不判），
      单测锁住（本仓 `isFinite(null)` 踩过同一类坑）。
    - **自己踩的坑 2**：起初把结果存进 `S.frames` —— 那个名字在 `replay.html` 里**已经是位置帧**
      （卡尔曼/CDF 用），会静默冲掉平滑与误差统计。改名 `S.stream` 并注明。
    - **实测（浏览器，30 分钟窗 / 780 帧）**：`780 frames · 0 gaps · 1 rollback · 0 duplicates ·
      median 2.0s / P95 3.0s`；那条回退就是**当天重启 UI 服务器**造成的（`seq` 从 564 回到 1，
      该行标 `seq rollback (restart / reorder)`）—— 若按 `seq - prev - 1` 天真实现，这里会显示
      「缺 −563 条」，正是这条要防的错。点第 41 行 → 光标从 22:17:45 跳到 22:19:15、
      `future` 779→739、面板同步重算（位置/RMS/搜索半径）；缺口与重复分支用合成行验证渲染；
      零控制台报错。
  - **未做**：
    **侧栏摄像头画面联动（最多 4 路 + 地图上画位置与视角，见 §五）**。
  - **无效时段处理（2026-09-12 用户选 A）**：窗口里常有「解不出位置」的大段时段（实测 30 min 窗：
    无观测 ~70% / 仅 1 台 ~3% / 可定位 ~27%）。做法 = **不静默截取**（缺口本身是线索）+ 进度条
    按「绿=可定位 / 橙=仅 1 台 / 灰=无观测」分色 + 顶部「有效 a% · 无效 b%」统计 +
    拉取后光标**落在首个可定位时刻** + 可选「跳过无效段」（跳过的缺口在轨迹里断开，不画假直线）。
    分段统计用 `pos.js` 的 `obsSegments()`（复用 `obsOfStation` 同一套判定）。
- [x] **重放/篡改自动化测试台（2026-09-12 完成）**：`orpah/run_checks.py` 一键跑全部检查 + 出报告。
  - **一键**：`python run_checks.py` 跑全部离线套件（现 **17 个**：`test_motion`/`test_keys`/`test_spoof`/
    `test_alerts`/`test_metrics`/`test_clock`/`test_tsdb_audit`/`test_server`/`checks_batch`/`test_levels`/
    `test_energy`/`test_capture`/`demo_clock`/`test_posjs`/`test_ratelimit`/`test_router`/`test_i18n`）；
    加 `--e2e` 再跑 **6 个**端到端 demo（L1/L2/L3/L3b/防 spoof/**限频**）；`--out` 指定报告路径；退出码可直接给 CI。
  - **报告**：`checks_report.md`（入库，同 `host/test_results.txt` 的惯例）—— 含 git HEAD、解释器版本、
    每套件结果/耗时/说明、关键输出行、失败套件的输出尾部 40 行。
  - **新增表驱动批量用例 `checks_batch.py`**（补“边界取值”这一类，与按模块写的 `test_*.py` 互补）：
    黄金样本（damm32=`B`/luhn32=`E`/mod97=`21`，并断言**里含 CC 就不对**、单字符改动/相邻换位必须被检出）、
    SN 边界 28 条（空/超长/小写 CC/CC 位数/Crockford 排除字符 I L O U/中文/多段/缺分隔符/前导空格…）、
    `sn_ok ⟺ sn_err is None` 一致性、`parse_sn` 四字段、报文编解码与以太网帧边界。
    **算法全部调既有单一源**（`damm32`/`luhn32`/`mod97`/`orpah_proto`/`registry`），本文件只写用例表。
  - **判定不只看出生码**：PASS = 退出码 0 **且** 输出里没有 `FAIL`/`Traceback`（双条件）——
    有些脚本自己 catch 了异常还会往下跑。已用四个探针脚本实测：正常/静默 FAIL/非零退出/自己吞异常
    四种情形判定全对。
  - **⚠ e2e 端口守卫**：`demo_l1..l4`/`demo_spoof` 与 orpah-ui（:8901 那一套）**端口会串扰** ——
    `--e2e` 前先检查 :8901，被占用就**直接拒绝（退出码 2）并提示先停 UI**，不制造“看起来失败”的假数据。

## 四、传输与真机适配

- [~] **无认证空口防 spoof 端到端演示（P1）**：开放空口 + 伪造 report 被拒 vs 合法签名 report 通过（结合 Damm32 + 签名）。
  - **已做（2026-09-12，主体完成）**：`spoof.py`（13 种攻击的构造 + 期望裁决，**脚本与页面共用一份**）+
    `demo_spoof.py`（**真·端到端**：搭 Client→STA→空口→AP→Router→UDP→Server 全链路，
    把每种攻击**真的注入空口**，在 Server 侧断言裁决 → 24 项检查全过）+ `test_spoof.py`（离线逐条自检）+
    页面入口（`index.html` 的 Orpah ID 卡片：选攻击类型 → 「注入伪造上报」/「跑全部攻击」，
    结果行显示「期望 X · 实际 Y ✔/✘」；完整清单与「被哪道防线拦下」见下面那个独立面板）。
  - **✅ 攻击流量独立面板（`attack.html` + `attack.js`，2026-09-13 完成）**：以前攻击结果只能混在
    首页的「签名上报流 + 事件历史」里看（正常周期上报与攻击报文排在一起，读者要自己猜哪条是哪条）。
    现在服务端按 **nonce 认领**攻击报文（`ui_server._spoof_track` / `_spoof_claim` → `/api/status.spoof`），
    攻击流量是**一条独立的流**（`id_reports` 照旧混排，`spoof.recent` 只装攻击 —— 实测跑完一轮后
    正常周期上报继续涨 5 条，面板一个数都不动）。面板包含：
    · **三种结果分开数**：与期望一致 / 与期望不一致 / **未等到结果**（`state=lost`，`ok=null`）。
      最后一种既不是“被拒”也不是“通过”：攻击报文同样受 §5.8 限频约束，而限频在**验签之前**，
      注入太密时它根本走不到验签 —— 写成“被拒”会让人以为防线抓住了；
    · **各道防线拦下条数** + 每条用例的「期望裁决（防线） / 实际裁决 / 被哪道防线」（同一栏里
      同时看得到期望与实际，不是“反正被拒了”）；
    · **只装攻击报文的流量流**（时间 / 用例 / SN / 级别 / 实际裁决 / 防线，SN 可点进清册）。
    实现口径：清单、说明（中英）、防线名、「错误码 → 哪道防线」全部取自 `spoof.py`
    （`CASES` / `defense_of()` / `DEFENSES`）；`test_attack.py` 从 `orpah_id.py` 抽出所有
    `_reject("<码>")` 逐个验证映射完备，并拦“页面里再写一份清单/拒绝码”的回归。
    联调实测：13 条用例经真空口跑完 5 s，13/13「与期望一致」，防线分布
    格式 1 / 级别 1 / 时间窗 1 / nonce 1 / 校验位 1 / 密钥库 1 / 验签 5 / 接受 2。
  - **攻击清单（14 条用例 = 13 种攻击 + 1 条合法对照）**：**格式非法（改报文头 `typ`，2026-09-13 新增，
    补上第一道防线此前无用例的空白）**、伪造签名（用别人钥匙签**被冒充设备的 SN**）、篡改时间戳、
    篡改设备自报观测、hdr 降级（ES256→HS256）、**能力降级（改 `payload.cap.rtc`，2026-09-13 新增）**、
    `alg=none` 免签冒充、SN 校验位错、
    未登记 SN、已吊销设备、重放、超窗、篡改路由器侧 xport（**已知边界，见下**）。
    防线顺序：格式 → SN 校验位（Damm32/mod97）→ 时间窗/nonce 去重 → 吊销表 → **设备公钥验签**
    （面板里按 `verify_report` 的**实际**评估顺序展示：代码里 SN 校验位在 nonce 去重**之后**）。
    演示价值：合法对照通过 + 每种攻击**被哪道防线拒**都如实显示（不是"反正被拒了"）。
  - **⚠ 本次暴露的开放问题（需记入 SPEC 待定项）**：**路由器侧观测不在签名预像里**
    （`xport` 由路由器转发时追加；设备的签名只覆盖自己的声明 `payload.seen_routers`）
    → 篡改 xport **验签照样通过**。而本 demo 的定位数据恰恰来自**路由器侧测量**
    （`root.orpah.routers.<sid>.<sn>`）→ **能冒充/控制一台路由器的人可以伪造定位**。
    这与"被改装的路由器"这一前提直接相关：路由器是**不可信**的（任何人可改），
    但服务器目前把它当可信上报方。缓解方向（待定）：路由器侧身份/签名（server 对路由器认证）、
    多路由器交叉校验、异常值检测。**动手前先与用户对齐**（可能超出 demo 边界）。
  - **✅ 已定（2026-09-13，用户定）**：就取**多观测一致性 + 离群剔除**（路线 b），并按
    **原则「孤证不立」**（§〇）执行 —— **任一 `xport` 永不单独采信**；1~2 个观测无冗余 →
    如实降级为「单一来源」；**k 台合谋不可防**（如实写明，不包装）；**移动目标**按各观测
    **各自时刻**比残差（否则正常走动会被误判成被篡改 → 假警报）。判据必须**在 `pos.js` 单一源里**，
    `track.html`/`replay.html` 共用；**可信度与精度分开显示**。
    规格落笔：`Protocol/docs/orpah-over-halow/SPEC.md` §8 原则 P-1 + 威胁 2 + §10 F-12（已结案）、
    `OrpahIDProtocol.md` §5.7 → **v1.18**（原「受信 router 集合」白名单**已否决**）。
    **✅ 演示场景已加到 4 台站位**（用户 2026-09-13 定）——`ui_server.DEMO_STATIONS` 四台，
    `_ensure_demo_stations()` **幂等补种**（老 `orpah.db` 里已有 3 台也能补上第 4 台，
    不去动用户改过的坐标）。第 4 台位置西侧 `(-20,0)` 不是随手放：按**「剔掉任意一台后剩下 3 台的
    几何」**（最坏 3 台子集 GDOP，在整条路线上取值）在候选里选的 —— 西侧 1.89，北侧 (0,40) 只能到 4.31。
    **✅ 页面接线已完成（2026-09-13）**：① 可信度三档显示（+ σ0 观测拟合度，与「精度」分开，
    见 `track.html` / `ui_i18n.js`）；② 「测距偏差倍数」注入入口——用于演示
    **“某台与其余台对不上”**（偏差成因不唯一：遮挡/多径、天线、元件、标定，也包括被改装/冒充；
    模型只假定空旷无遮挡）；③ **三幕演示（量化）**：同一位置各测 20 次 →
    「① 正常 / ② 1 台×4（被带偏 → 剔除 → 订正）/ ③ 2 台×4（**定不了是哪台**，只看 σ0）」，
    实测 ② 被带偏 ≈136 m → 订正 ≈3.8 m、19/20 次有剔除；③ σ0 0.38 → 1.71（阈值 1.8，可能压线不告警）。
    **④ 回放页也呈现可信度（2026-09-13）**：逐帧判定（光标时刻）+ **整窗统计**（可定位帧里
    「交叉校验通过/单一来源/冲突」各占多少、剔除离群与拟合偏松各多少帧、σ0 中位）。
    判定与文案都走 `pos.js`（`consensus()` + `consOpts()` + `trustText()`）；
    观测时刻 `obs[i].ts` 由 `obsOfStation` 带回，回放用因果滤波速度做**运动补偿**（否则正常走动会被判成冲突）。
    **⑤ 轨迹与进度条按可信度着色（2026-09-13）**：绿实线=交叉校验通过、橙虚线=单一来源、
    红点线=冲突（颜色+线型双通道，红绿色盲可辨）；进度条从“能不能定位”改为按可信度分档；
    平滑轨迹改中性灰虚线（绿已专表“可信”，且 平滑≠更可信）。配色单一源 `TRUST_COLOR`/`TRUST_STYLE`。
    **术语口径**：禁用“谎报/撒谎/作弊”等归因性词，统一「偏差 / 不一致 / 离群」
    （规则写在本仓 `AGENTS.md` §0「孤证不立」条内 + 规格 `SPEC.md` §8 威胁 2 条 1b）。
  - **未做**：真实空口（需硬件，见 §四其余项）。
    （**攻击流量 UI 独立面板已于 2026-09-13 完成**，见本节上面那条。）
- [x] **Router 下行真实性（F-14，2026-09-13 完成：A 来源校验 + B 签名）**：Server → Router 的三类下行（LOST-TABLE /
  TRACKING-STATUS / ERROR）**都未签名** → 能向 Router 的 UDP 端口发包的人可以送一张**伪造的
  LOST-TABLE**：它会整体替换走失缓存并置 `_synced=True` → 此后不再拉表、命中也不答 TRACKED、
  **不再产生 ORPAH-FOUND**（最阴的版本：只摸掉某个 SN，其他一切正常）——
  即**一条伪造的空表就能“弄瞎”这台 Router**。
  - **已做（A 方案，用户 2026-09-13 选）**：`router._from_server()` 下行**只收配置的
    Server 源 IP + 源端口**，别的在**解析之前**就丢 + 计数/留痕（`down_rejected`、
    `/api/status.router_down_rejected`、页面 Router 行）；`server_host` 为主机名时先解析。
    **fail-closed**：真机若源地址与配置不同（NAT/多宿主）表现为“下行全被丢”，显式失败。
    验收：`test_router.py`（9 项离线）+ `demo_l4.py` 真 UDP（从 `127.0.0.2` 打假表 →
    被丢且缓存不动；对照组 Server 来的照常生效）。
  - **✅ 已做（B 方案，2026-09-13 完成）**：`downlink.py` —— **Server 用私钥签名，Router 只持公钥**。
    - **为什么不用 HMAC 共享密钥**（本轮最重要的取舍）：威胁模型就是「Router 可被改装」——
      共享密钥一旦放进 Router，**改一台 Router 的人就拿到了给别的 Router 发假表的密钥**；
      公钥不是秘密 → 改装一台学不到伪造能力。代价是下行多一次 ECDSA 验签
      （下行条数很少：拉表应答/回执/错误），**划算**。
    - **签名在唯一出口**（`server._reply`）：以后新增下行类型不会漏签 ——
      漏签的后果是 Router 直接拒收（fail-closed，可见），而不是默默降级。
    - **预像盖住整条报文**（JCS，去掉 dsig）：entries/rid/status/sn 改任何一个都验不过。
    - **必须带新鲜性，否则等于没修**：报文带 `dts`（epoch 秒）+ `dn`（Server 单调计数），
      Router 要求 `|now−dts| ≤ ORPAH_DOWN_WINDOW`（默认 300s）**且 `(dts,dn)` 大于上次已接受的那对**
      （按类型各记）→ **重放一张旧空表**（重放不需要私钥）被拒；Server 重启 dn 归零也能过（dts 更大）。
    - **两条边界照实**：① **没配公钥 ≠ 已启用** —— Router 照旧收下（不把 demo 搞挂）但计数
      `down_unverified` + 日志吵一次 + 页面显示「未启用（只有来源校验）」；有公钥则 fail-closed
      （`no_sig`/`bad_sig`/`stale`/`replay` 全拒）。② 只管 **Server → Router**，
      **空口那段（Router → Client）仍无认证**（低功耗客户端验不了）；私钥保管不在范围
      （demo 是 PEM 文件，真机要硬件信任根）；能长期做中间人的人仍可丢弃/延迟下行、
      也能在窗口内重放**更新**的那条 —— 要挡这些得靠 TLS/持久会话。
    - 验收：`test_downlink.py`（44 项）+ `test_router.py`（配了公钥后的无签名/篡改/错钥/重放/超窗）
      + `test_server.py`（三类下行都带签名、dn 单调、回头重放被拒）+ `demo_l4.py` 第 ⑨ 组
      （真 UDP：**同源套接字**（IP+端口都与 Server 相同，A 认不出）的无签名空表被丢、缓存一动没动；
      伪造签名被丢；重放被丢。**这组就是 B 存在的理由**）。
  - **仍未做**：下行**重试/持久会话**（要挡“中间人丢包/延迟”得靠 TLS，不在 demo 范围）、
    私钥的**硬件信任根**（真机）。**注：源地址校验 ≠ 真实性** —— A 单独用时不得写成“已防住”，
    现在两者一起才谈得上；跨网段/NAT 下 A 会 fail-closed（下行全被丢，可见）。
- [ ] **IPv6（Phase 2 / 真机阶段，未做）**：本链路 Phase 1 **只做 IPv4**（每处 socket 都显式建
  `AF_INET`，全仓无一处 `AF_INET6`），IPv6「未做」**不是缺口**（2026-09-13 记录）。
  两条**实际影响**（别当无关细节）：
  ① 它是「下行来源校验（F-14 A）**不必考虑 IPv6 变体**」的前提 —— Router 的 UDP socket 是
     `AF_INET`，IPv6 源**到不了**那个 socket；
  ② **绑定地址是演示选择**：模拟器/Server/UI 都绑 `127.0.0.1`，而 **Router 的 UDP socket 是
     未绑定即发（`0.0.0.0:<临时端口>`）** —— 真机必须把 Server 绑到实际网卡地址并把 `server_host`
     指向它，否则 A 的 fail-closed 会表现为“下行全被丢”。
  要上 IPv6 得一起改**五处**（模拟器三处监听 + `host_bus` 连接 / `server.py` 的 bind 与
  `router.py` 的 socket 族 / `router._from_server` 的 `gethostbyname`→`getaddrinfo` 与
  **归一化地址比对** / 限频 per-Router 桶的 key / demo·测试里写死的 `127.0.0.1`）。
  **规格落笔：`Protocol/docs/orpah-over-halow/SPEC.md` §6 + §8 威胁 4 前提；本仓 `AGENTS.md` §0。**
- [x] **限频（《Orpah ID 协议规范》§5.8，P1）**：签名只能滤掉“伪造”，滤不掉“**洪水**”——
  合法设备被高频刷量时，每条都要走一次 ECDSA 验签，不设限就是自伤。
  - **已做（2026-09-13，四行全部）**：
    · **Server 侧**（限 CPU）：`ratelimit.py`（令牌桶；纯 `now` 函数，**不睡眠不轮询**）+ 接入
      `server._handle`，位置在**验签之前**：**per-SN + per-Router** 两条都过才放行（被拒的请求
      不吃好设备的额度，且整段持锁 → 判定原子），丢的另记 `ratelimit` 事件（**不**记成 `id_reject`）；
    · **Router 侧**（限带宽）：转发**按 SN** + 未签名的 REQ-CONNECT **按源 MAC**（`eth_src_mac`）；
    · **设备侧（客户端，2026-09-13 补）**：`ratelimit.DeviceLimiter`（复用同一个令牌桶）+
      `ClientHost._gate` 三处上行接线（`ORPAH_SELF_*`，默认最小间隔 0.6s / 突发 4）。
      ★ **它不是防线，是自愿的** —— 被改过/失控的设备不会做（协议也要求不了）；
      好处只有三个：空口是共享介质（帧**已经上过空口**，带宽已花掉）、省电、不撞上游的桶。
      语义是**延后（hold）不是丢弃**（丢自己的报 = 漏报）→ 页面/文案用「已延后」这个词。
      默认值按真实节奏定（一个周期 3 条挤在 0.25s、然后歇 2s → 长期 1.5 条/秒；
      0.6s≈1.67 条/秒 高于它 → 正常永不触发，又低于 Server 侧 2 条/秒 → 不撞上游桶）。
    · **两个例外，永不限频**：`ORPAH-FOUND` 与**命中走失表的 REQ-CONNECT**（漏一条 = 一个人没被找到）；
    · `id_ratelimit` warn 告警（默认 60s 窗口，**只陈述事实不归因**）；
    · 页面限频卡片分**两侧**显示（参数/计数/最近丢弃 + 侧列）+ 「刷量（同一 SN / 轮换 SN）」按钮，
      参数一律读 `/api/status.ratelimit(.params)` / `ratelimit_rtr`，**页面不写死**。
  - **验收**：`test_ratelimit.py` **34 项**（桶数学/隔离/上限淘汰/并发原子性/两侧接线/
    “转发不吃 MAC 桶”/“命中走失表不限”/两侧“默认参数零丢弃”/“限频不是封禁”）+
    `test_selflimit.py` **24 项**（设备侧：桶数学 / 延后不扣令牌 / `wait_for` 只算不动 /
    环境变量三套前缀隔离 / **“真实节奏 300 周期零延后”回归锁** / `force` 不扣令牌 /
    被延后时**不发** / 文案口径守卫）+ `demo_ratelimit.py`（真链路 **22 项**，含“同一 SN 连发 200
    → Router 侧丢 159 / Server 侧丢 35”的分层证据、“12 条 FOUND 一条不丢”、以及第 ⑦ 组
    **设备侧自限频**：守规矩 20 条 → 发出 4 / 延后 16、上游两侧**零丢弃**；同节奏 `force=True`
    对照 → 上游丢 16）。
  - **实测踩过的坑**：① 把源 MAC 传给转发检查 → probe 桶（1/s）把 `demo_spoof` 的 13 条连发
    当探针限掉（e2e FAIL）→ 行 2 只按 SN；② 只等 Server 侧计数 → Router 侧一开就永远等不到
    “这批处理完”（要两侧合计对账）；③ 桶表淘汰用 `min()` 扫描 → 满表时 **219 µs/条**
    （无淘汰路径 5.2 µs/条）→ 限频层自己变 CPU 放大器 → 改 `OrderedDict` LRU。
    ④ 设备侧：`TokenBucket` 的时钟必须与调用方 `now` 同一套 → 必须惰性建桶
    （测试传假时钟 `now=0` 会被算成“负时间差” → 满桶瞬间扣光，60 条全被延后）；
    ⑤ 演示“守规矩的设备不撞上游桶”前，**必须先等上游桶回补到够这一段突发**
    （只等“有额度”不够：桶边缘 1 个令牌仍会丢剩下的；完全不等 → 头 3 条全丢，
    看起来像自限频没用）→ 新增只读观测 `_Buckets.tokens()`。
  - **未做/做不到的（如实，不得写成“防住了”）**：① per-SN 的 key 取自**尚未验签的 `sn`**
    → 轮换 SN 就能绕过（仅剩 per-Router / per-源MAC 兜；换源则都能绕）；
    ② 命中走失表的 REQ-CONNECT 不限 → 刷“已知走失 SN”可绕开 probe 那行（为“不漏报发现”付的代价）；
    ③ 多条源同时刷 → 各条防线都会到顶，只能靠上游/接入侧；
    ④ 两个桶的默认数值都是演示/开发配置，**未经真机标定**（同 F-11 性质）；
    ⑤ 页面上轮换刷量看不出 per-Router 拦截（仿真空口几 条/秒 < 桶补充速率，已在页面如实标注）；
    ⑥ **设备侧自限频不是防线**：只能约束**守规矩**的设备，被改/失控的设备根本不做它
    （demo 用 `force=True` 把这条边界直接演出来）；它的默认值同样是演示配置。
- [x] **Wi-Fi 抓包 → ORPAH 报文解析** 与 **HaLow/UDP 模拟器**双源对照（2026-09-12 完成）：
  - 新增 `capture.py`：classic pcap 读写 + 逐帧解析 + **双源对照**（`compare()` = 纯计数差集，
    `diff`/`only_capture`/`only_ref`）。解析**复用协议层单一源**（`orpah_proto.parse_eth_frame`
    + `decode_msg`），不在工具里另写一套帧/报文解析。
  - **格式错误要吵不要静默**：pcapng（Wireshark 默认新格式）→ `PcapError` 并给出
    `editcap -F pcap in.pcapng out.pcap`；截断/魔数不符/非对象参照 JSON 同理。
    （静默跳过会把“格式没支持”错当成“抓包里没有 ORPAH 报文”。）
  - 非 Ethernet 链路层（Radiotap/Linux SLL 等）**明确警告**并说明需先按该链路类型解封装，
    而不是甩一堆 `not-orpah`。
  - **双源对照只算差、不猜映射**：参照计数由调用方自己从它的来源（`/api/status`、IoTDB 事件数）
    数出来（`--ref-json`），因为“哪些计数该与哪些报文对齐”是业务口径。
  - CLI：`python capture.py --pcap orpah.pcap [--limit N] [--json] [--ref-json ref.json]`；
    真机抓包：`tcpdump -i eth0 -s 0 -w orpah.pcap 'ether proto 0x88b5'`。
  - 测试：`test_capture.py`（27 项：往返字节一致 / 四类格式错误 / 五类报文计数 / 脏帧不连坐 /
    ID 报文抽 alg+level / 对照四情形 / 参照 JSON 容错 / CLI 退出码），已并入 `run_checks.py`（离线 11 套件）。
  - **诚实边界**：没有能抓 HaLow 空口的嗅探器时，只能抓 **RJ45/网口侧**或用模拟器自己写出的 pcap；
    后者验的是**解析器与对照逻辑**，**不能替代真机抓包**（真机侧留待阶段二上机时用）。
- [ ] **多租户/多组织**：SN 按组织隔离、角色权限（看客/运维/管理员）。（与 §三「案件经办/分配」同族：那里的 L3 就是本项；**做 L3 前先确认 L2 是否真需要**）
- [x] **时钟可信：无 RTC 设备的 ts 处理（2026-09-12 完成）**
  - **现状盘点（开始前）**：协议层已经对了 —— `orpah_id.verify_report` 对 `ts=0` **跳过时间窗**
    （仅靠 nonce 防重放，§5.5）。缺的是**下游一致性**：`write_report` / 路由器观测写入 / `registry.touch`
    各自用 `time.time()` 兜底（三处各取一次、毫秒级偏差），而报文流页面仍显示 `ts=0`→1970。
  - **做法（单一归一化点）**：`orpah_proto.effective_ts(ts, rx)` → `(ts_int, src)`：
    设备时间可用（整数、非 0、≥2000-01-01、不超前服务器 >1 天）→ 用它（`src=device`）；
    否则（ts=0/缺失/非整数/bool/荒谬）→ 用**服务器接收时刻**（`src=server`）。
    `ui_server._on_report` **一次**算出，然后设备流 / 路由器观测序列 / `registry.touch` / 审计 / 展示
    全用同一个值 —— 实测设备流与三台路由器观测落在**同一毫秒**（以前只是碰巧接近）。
  - **留痕（审计要求）**：`src=server` 时在事件 detail 写 `ts_src=server`（`id_report`/`id_reject`）、
    报文流该行带 `ts_src`/`ts_eff`（页面显示服务器时刻并加 `*`、悬停说明）、Orpah ID 卡片显
    「设备无时钟（ts=0）」。**否则事后分不清「设备说的时间」与「服务器看到的时间」**。
  - **不改报文**：`ts` 在签名预像里，改了验签就不过 —— 归一化只用于**我们记录**的时间（原始 ts 保留）。
  - **测试**：新增 `test_clock.py`（归一化规则表驱动 11 条 + 验签窗口 5 条 + 进程内 UDP 端到端 6 条：
    ts=0 被接受、`ts_src=server`、`ts_eff` 落在“现在”附近；ts=now → `ts_src=device`）。
    已并入 `run_checks.py`（现 8 个离线套件，8/8 通过）。
  - **时钟漂移滤波（2026-09-13 完成，`clock.py` + 告警 `id_clock` + `demo_clock.py`）**：只用
    「设备自报 ts vs 服务器接收时刻 rx」这对观测，反推 `offset = device−rx` 与 `drift_ppm`（最小二乘斜率）。
    **只估计、不改数据**（`ts` 的归一口径仍归 `effective_ts`，§5.5）。四处**实测**踩坑与修法：
    1. **偏移要反应快**：起初偏移也用整窗（64 条）中位数 → 现场拨偏 +120s 后**一分钟内读数仍≈0**
       （旧样本压着中位数），演示像坏的、告警也不亮。改成**最近 `OFFSET_WINDOW=8` 条**取中位数
       （状态量）：实测拨偏后 ~8s 即读回 `119.5`，拨回后 ~6s 恢复 `-0.3`；单点跳变仍免疫。
       整窗/最近窗两个散布都给出：`spread`（当前抖动 ~0.77）与 `spread_win`（整窗极差，只是指示器）。
    2. **跳变不是漂移**：窗内一次拨表（±120s）会被斜率读成 **~2,000,000 ppm** → 且因为
       `|drift|>阈值` 告警会**赖着不走**（实测拨回后仍持续 60s+）。改成：按“两侧各 3 条**中位数**之差
       超过偏移告警阈值”把序列**切成段，只用最新一段**拟合 —— 单点乱报不算跳变（不切段），真拨表才算。
       实测修后拨回 10s 内告警消失。
    3. **噪声里看不出趋势就别给数**：设备 `ts` 是整数秒（截断）→ 单条偏移有 ~0.29s 抖动，而
       `σ_slope ≈ σ_resid·√12/(span·√n)`。`demo_clock.py` 实测：60s 基线 σ≈4100ppm、10min≈110ppm、
       1h≈9ppm ⇒ **晶振级（200ppm）要 ≥1h 基线**才说得上话；门限用 `3σ` 自适应，不写死 ppm。
       `drift_why` 明确区分 `min_span`（基线不足）与 `noise`（看不出趋势），页面直接显示原因。
    4. **单点乱报不能拖偏斜率**：普通最小二乘被一条重启垃圾 ts 拖偏几万 ppm → 拟合前**按时间分箱取
       中位数**（≤32 箱、每箱≥8 条），乱报进不了拟合，且对 iid 量化噪声基本不损失分辨力。
    - 基线长度：环形窗 64 → **4096 条**（1s 上报≈68min、5s≈5.7h）。
    - 告警：`id_clock`（warn，偏移 > `ORPAH_ALERT_CLOCK_OFFSET_SEC`=30s 或漂移 >
      `ORPAH_ALERT_CLOCK_DRIFT_PPM`=200ppm），`since`=首次越界时刻，数据字段带 `offset_sec`/`drift_ppm`/`n`。
    - 演示：`demo_clock.py`（合成 A~F 六个场景：准/偏快/短基线/拨表/拨表后恢复/乱报 + 分辨率随基线变化，
      毫秒级跑完“几小时”）；首页「上报控制」卡片可现场拨偏并看估计值与“为什么没给漂移”。
    - 测试：`test_clock.py` **64 条**（偏移中位数/漂移斜率/短窗不给漂移/单点跳变免疫/拨偏快反应/
      跳变分段/噪声不给漂移/真漂移不被门挡/分箱鲁棒/并发），`test_alerts.py` 加 `id_clock` 12 条 + 2 个环境变量。
  - **能力位：设备自报「有无 RTC」（2026-09-13 完成，协议 + 服务端 + 告警 + 演示）**：
    以前 `ts=0` 只能**反推**“它可能没有时钟”，分不清「设备本来就没有 RTC」（免电池终端，正常）
    与「有 RTC 却坏了/被动了手脚」（该有人看一眼）。现在由设备**自报能力位** `cap = {"rtc": true|false}`：
    - **三态**：`true`=有 / `false`=无 / **缺省=未声明**（未声明 = 老设备走老行为，**不能**当成无 RTC）。
      只归一化白名单字段（`CAP_FIELDS`），未知能力位丢弃；JSON 对象而非位运算（加能力不用改协议位图）。
    - **防篡改**：`cap` 放进 ID 报告的 `payload` → 在 **JCS 预像**内 → 改它就验签失败。
      攻击者把 `rtc:true` 改成 `false`（想把“异常时钟”伪装成“本来就没时钟”）= **降级攻击**，
      新用例 `spoof.cap_downgrade` 专门演它（清单 12 → **13 种**；2026-09-13 又加 `bad_format`（改报文头
      `typ`）给第一道防线补上用例 → 现 **14 条用例 = 13 种攻击 + 1 条合法对照**）。
      业务报文 `ORPAH-REPORT` **未签名**，其 `cap` 只能当**提示**（用于选时间口径），不参与告警。
    - **服务端策略**：声明 `rtc=false` ⇒ **一律**用服务器接收时刻（即使 `ts` 看着正常 ——
      没有参考时钟的设备报的“时间”不是时间基准），且**不喂给时钟估计器**（否则估出常数/噪声）；
      `ts_ok=false` 留痕。
    - **告警 `id_cap_mismatch`（warn）**：**已签**声明「有 RTC」却送出不可用 `ts` → 设备故障/被动过手脚；
      反之「无 RTC + ts=0」是**正常，不告警**。状态型 → 同一台只出一条、窗口内没再犯自动消警
      （`ORPAH_ALERT_CAP_MISMATCH_SEC`=300s）。
    - **两处实测踩坑（都写进代码注释 + 回归测试）**：
      1. `server.py` 里误把 `cap_of()`（返回**规范化 dict** `{"rtc":true}`）当 `rtc` 传给
         `effective_ts` / 写进记录 → `rtc is False` **永不成立**（“无 RTC ⇒ 服务器时刻”**静默失效**），
         且 `alerts` 的 `cap_rtc is True` 也对不上（**告警永不出**）。必须取**三态布尔** `rtc_of()`。
         页面实测才发现（单测当时只断言了 UI 字段，没断言存储字段）→ 补 `test_server` 5 条回归。
      2. 告警 `since` 必须用 `ts_eff`（epoch 秒），**不能**用 `rec["t"]`（`"15:46:21"` 展示字符串）——
         `int()` 会抛，`/api/alerts` 整个 500（`sig_fail_rate` 踩过同一个坑）。
    - 演示：首页「上报控制」卡加 **设备能力** 下拉（未声明/有 RTC/无 RTC）+ **ts 置 0** 复选框 +
      时钟行尾显示「有/无 RTC · 时间用服务器接收时刻」；审计 detail 带 `cap_rtc=1|0`。
    - 测试：`test_clock.py` +24 条（`cap_of` 归一化 9 + 三态 1 + 策略 5 + `ts_usable` 8 + 同源 1，共 **88** 条）、
      `test_alerts.py` +15 条（含 4 个环境变量）、`test_server.py` +5 条（共 23）、
      `test_spoof.py` +3 条语义检查（逐条裁决清单自动多一条）。
  - 未做：**真机侧探测 RTC 是否真的存在**（协议字段已就位，等上机联调：真机自报 `cap.rtc` 后看是否与实测一致）。
    ~~跨小时的长基线漂移序列~~ 已由第 3、4 条（长环形窗 + 分箱鲁棒拟合）覆盖。

## 五、可观测与交付物

- [ ] **回放视频联动（摄像头画面）**（2026-09-12 用户提，**只记录、不做**；见下面细节）：
  回放页右侧显示**最多 4 路**摄像头画面，画面时刻**随回放进度联动**（人走到哪里，那一秒的画面就出哪里）；
  并把摄像头的**位置与视角（视锥）**在地图/画布上画出来。
  - **待定设计问题（动手前先定）**：
    1. **视频源形式**：演示优先用**本地文件**（mp4/webm，可 seek、可变速）；真实系统是 RTSP/HLS ——
       浏览器**不能直接播 RTSP**，必须服务端转封装（HLS/fMP4/WebRTC），属另一条工程线。
    2. **时刻对齐（核心）**：每路摄像头需要**录像起点 epoch**（+ 时区/偏移）→
       `videoTime = 回放光标时刻 − 录像起点`；seek 时 `video.currentTime = videoTime`，
       倍速用 `video.playbackRate = 回放倍速`，暂停就全暂停。没有起点时间戳就谈不上联动。
    3. **摄像头元数据存哪**：新增 SQLite 表（与 `stations` 同级）：`sid/name/x/y/朝向角/FOV/可视距离/录像起点/源地址`。
       按「摄像头 = 另一类传感器」建模，不要塞进 `stations`（站位是测 RSSI 的点，语义不同）。
    4. **视角绘制**：位置点 + 视锥扇形（朝向 ± FOV/2、可视距离）——画布用 `ctx.arc`，地图用 `L.polygon`；
       与现有站位/椭圆同属一层。
    5. **性能**：4 路同时解码很吃 CPU/内存 → 只播「当前时刻在录像区间内」的几路、`preload="none"`、
       切时段时先 `pause()` 再换 `src`；画布/地图上的摄像头标记与回放同一签名更新。
    6. **隐私与边界**：视频是最敏感的一类数据（人脸 + 位置）——
       demo 里**只用自制/无隐私的示例视频**；真实部署涉及人脸识别、留存时长、调阅授权，
       均**超出本 demo 边界**（与 §〇 范围原则一致：这属于「提升态势感知」，不是「让找人更快更准」的必要条件）。
  - 依赖：先有「移动的人」的样例数据（人不动、只有 4 路画面联动是看不出效果的）。

- [~] **指标面板**（2026-09-12 完成 4/5 项，页面 `metrics.html` + `GET /api/metrics`）：
  - **已完成**（数据来源全部现成，纯计算抽到 `metrics.py` + `test_metrics.py` 单测）：
    ① **签名校验失败率** = `id_reject / (id_report + id_reject)`（事件流，时间窗）；
    ② **签名算法分布** = 按审计 detail 里的 `alg=` 统计（**唯一需要解析 detail 的地方**，取不到归
       `unknown`、不猜）；③ **平均 RSSI** = 设备上报流的均值/最小/最大 + 样本数（时间窗）；
    ④ **走失处置时长** = 立案→首次发现、立案→结案（均值/最小/最大 + 逐案明细）。
  - **口径纪律**（写进 `metrics.py` 头注释）：无样本给 `None` 不给 `0`（0 dBm / 0% 失败率都是
    "看起来验过了"的假象）；**未结案的案件不进均值**（否则均值随等待时间漂移），未结案单列计数 +
    逐案 `elapsed`；**案件不按时间窗切**（一个案子跨小时，窗口化会把时长算错）——窗口只作用于
    事件流与上报流。
  - **未做：定位误差 CDF（阶段二）** —— 误差需要**地面真值**，而真值只有模拟器里有
    （`motion.py` 的行走模型）。做法 = 接口暴露真值轨迹 + 页面用共享定位内核 `pos.js` 逐帧算估计
    → 画 CDF；`--no-walk` 无真值 → 显示「不适用」。**动手前先定**：真值是否只该出现在演示里
    （真实部署没有真值，指标口径要写清"仅模拟环境"）。页面该卡片已如实标「未做」。
  - **已做（2026-09-13，定位误差 CDF —— **仅模拟环境**）**：接口暴露真值轨迹（`/api/truth`）
    + 页面用共享定位内核 `pos.js` 逐帧算估计 → 画 CDF（`replay.html` §1b）。
    - **口径已定（这就是当初要“先定”的那条）**：真值**只出现在演示环境**（`motion.Walk` 行走模型），
      **真机部署没有真值** → 该指标**不是产品指标**；页面如实标「仅模拟环境」，真机看**不需要真值**的
      残差 RMS / GDOP / 95% 椭圆 / 搜索半径。`--no-walk` / 无真值 → 显「不适用」并说明原因。
    - **踩坑（自己造数据坑自己）**：一开始只做“网格采样 + 页面线性插值”，还写了“误差 <1cm”的注释 ——
      实测 **250ms 步长时插值误差就有 2.9cm**，而且随步长**平方**增长：12h 窗口被点数上限（4000）
      逼到 10.8s/点 → **米级**，真值自己变成误差源。改为 `POST /api/truth {times:[…]}` 按**帧时刻
      精确取点**（`motion.truth_at`），CDF 不再经过插值；网格 GET 只留给“画真值路线”。
    - 实测（演示数据 30min 窗）：原始 P50 **4.4m** / P90 8.6 / P95 9.8 / max 20.6；平滑后
      P50 **3.5m** / P95 8.4 / max 18.2 → **平滑把 P50 降 ~1m**（与上面卡尔曼那条一致）。
    - 测试：`test_motion.py` 加真值采样/取点 9 条（与 `walk.pos` 逐点一致、上下限、插值误差与步长
      平方增长的实测）；**新增 `test_posjs.py`**（node 跑 `pos.js` 原文，22 条：RSSI 往返/三边/WLS 降权/
      椭圆/观测归集不看未来/卡尔曼因果/**CDF+分位数+真值插值**）→ 并入 `run_checks` 离线套件。
    - **已做（2026-09-13，真值/估计叠加导出 —— 指标面板这一条收尾）**：
      导出下拉新增第四个来源 `cmp`（对照 = 真值 + 原始 + 平滑）+ 第三种格式 **CSV**。
      - **分工（刻意分开）**：GPX / GeoJSON 是**地图格式** —— 一个文件里放三组线段
        （`properties.source = truth|raw|smoothed`，GPX 三个 `<trk>`，各自按缺口分段），
        用来“**看哪儿偏了**”；CSV 是**逐帧对照表** ——
        `t_ms,time,truth_x/y,est_x/y,err_m,sm_x/y,sm_err_m,truth_lat/lon,est_lat/lon`，
        用来“**偏了多少、能不能算**”。比 CDF 多出来的正是**时段定位**（误差大在哪一段）。
      - **配对与误差 CDF 同一份**：`S.truthIdx[k]` → `S.frames[i]`/`S.sm[i]`/`S.truth[k]`
        （真值按帧时刻精确取点、不插值），所以面板上的 P50/P95 与导出表逐行算出来的一致。
      - **没有真值就不静默退化成单源**：选 `cmp` 当场提示“不适用”+ `truthWhy` 原因，
        按下导出也**不产文件**（实测拦截下载计数 = 0）。真值只有模拟环境有，真机继续看
        不需要真值的 RMS / 椭圆 / 搜索半径。
      - **CSV 里缺口不补行**：`t` 直接跳（时间戳自己会说话），不插值、不补空行 —— 与“缺口断开”
        同一纪律，只是换了个载体。
      - 实测（浏览器，30 min 窗）：逐帧配对 **773 行**；误差自洽（`|hypot(est−truth) − err|`
        逐行 < 1e-9）；三条轨迹各分成 2 段（窗内有缺口）；GPX 3 个 `<trk>`（名字 `sn` / `sn truth` /
        `sn raw` / `sn smoothed`）、GeoJSON 6 Feature 且 `source` 覆盖 truth/raw/smoothed；
        CSV 表头 14 列 / 773 数据行，首行 `truth(8.166,4.083) vs est(5.070,0.000) err 5.124m`
        且经纬度与基点换算一致；零控制台报错。
    - **未做**：真机的“无真值质量指标”口径已定（残差/椭圆/搜索半径），无需真值。
- [~] **计数口径（2026-09-12 代码复核：① 已修，②③ 为有意不加锁）**：
  ① ✅ **已修（2026-09-12）**：路由器「收到走失表 N 次」原来靠 `_pull_pending` 时间窗（≤1.5s）
     区分「主动拉表应答」与「Server 主动推送」——推送恰好落在窗口内会被当成应答（少计 1 次），
     反之多计。现在 `ORPAH-LOST-TABLE-REQ` 带**关联号 `rid`**（`orpah_proto.new_rid()`），
     **应答原样回显**、主动推送不带 → 确定配对（不匹配的旧/别台应答按推送计）。
     两端语义：Router「收到走失表」只计**Server 主动推送**；Server「发布走失表」只在
     mark/untrack 变更时计 → 两边能对上。协议做法已回填 `Protocol/docs/orpah-over-halow/SPEC.md`
     v0.7.1（§5 字段表 + §7 拉表时机 + §9 L3b）；`demo_l4.py` 加 6 项 rid 检查（共 **11** 项全 PASS），
     含**旧近似回归锁**（直接喂三类表：应答计 0 / 推送计 1 / rid 不匹配计 2）。
     **2026-09-12 复核加固**：① 把单个 `_pull_rid` 改成**在飞 rid 集合**（`deque(maxlen=8)` + 锁）
     —— `start()` 先起两个循环线程再 `sync()`，且 `_handle_up` 也可能再调一次
     → **两次拉表可能重叠**，单变量时“读-判-写”非原子会让先到的应答掉配对（多计 1 次）；
     集合天然支持多 in-flight（新增回归锁：两个 rid 先后回都算应答）；
     ② `_apply_lost_table` 从“先清空再逐条填”改成**先建新表再整体替换**（去掉“中间一瞬空表”
     的窗口 → 那瞬间来的 REQ-CONNECT 会误答 NOT-TRACKED）；
     ③ `demo_l4` 里的 `sleep` 换成 `wait_until`（本仓“不猜次数等待”的规矩）。
  - **Router 线程模型（复核时明确，供下次别再问）**：`router.py` 有 **3 个线程** ——
    `_air_loop`（收空口帧 → `_handle_up`，会读 `_synced`/`lost_cache`，可能调 `sync()`）、
    `_udp_loop`（收 Server 下行 → `_apply_lost_table`，写 `lost_cache`/计 `lost_push_recv`）、
    调用方线程（`start()`/`REQ-CONNECT` 路径调 `sync()`，写 `_pull_rids`）。
    现状：`_pull_rids` 有 `_pull_lock`；`lost_cache` 改成**整体替换**（不再逐条写）；
    `lost_push_recv += 1` 只在 `_udp_loop` 单个写者里 → 不需锁；
    `_synced` 布尔单写读（最坏多拉一次，无害）。
  ② `status()` 与各回调线程共享内存结构（`reports`/`order`/`flow`/`id_reports`…**无锁**）：
     已修掉唯一会真抛异常的路径（`reports[seq]` → `reports.get(seq)` + 跳过，
     见 `ui_server.status()` 注释；用真实线程复现过 `KeyError: 4`）。
     其余是「读迭代最多跳一条、下一轮轮询补齐」级别，**故意不加锁**（每个结构单写者，
     GIL 下单条操作原子；加锁会把 5 处回调都拖进同步）。将来真做多客户端并发再上锁 + 快照。
    ③ 同类：`_ensure_id_device`（重建演示设备）与上报线程的 `_id_tick` 共享 `id_dev`/`id_ks`，无锁；
    最坏后果是某一轮 `id_demo` 卡片显示一条错误（`_id_tick` 有 try/except），下一轮自愈 —— 同样不加锁。
- [x] **配置中心（2026-09-12 分项落地）**：原条目「路径损耗 A/n 校准值、各 router 坐标、锚点库、
  基图参数、SSE/API 地址外置」逐项对待如下 ——
  - ✅ **A/n 校准值单一源**：原先 A/n 在**四处各写死一份**（`motion.py` 正向算、`track.html` /
    `rssi.html` / `replay.html` 反算），靠注释「与页面默认一致」互相提醒 —— 改一处不改另一处
    就会**静默**用不同模型反算距离（定位/椭圆偏了还不报错，实时页与回放页互相矛盾）。
    现在 `motion.py` 为唯一源 → `GET /api/config`（§11）→ 三页开页填默认值（仍可手改）；
    页面 HTML 里的 `value=` 降级为离线兜底，`test_motion.py` §8「单源守卫」锁死兜底值
    == 常量且页面确实去取接口。（`noise_db` 一并暴露，但**刻意不绑**页面自己的仿真噪声旋钮：
    那是浏览器内造数据的旋钮，语义不同。）
  - ✅ **各 router 坐标**：早已可改 —— `/api/stations` 的 `add/update/remove/clear/import`
    （`track.html` 站位表里直接编辑 x/y/名称/时间窗）。本轮只核对，无改动。
  - ✅ **基图参数**：早已单一源 —— `map.js` 一处定义 providers/licence/`toLatLng`/离线兜底，
    `track.html`/`replay.html` 共用（不各写一份）。
  - ❌ **锚点库**（预置若干套「无人机悬停计划」一键载入）：**未做**，需要先有真实悬停计划样本
    才知道模板该长什么样；现在用「导入悬停计划 JSON」（`/api/stations` 的 `import`）兜住。
  - ❌ **SSE/API 地址外置**（把 `http://127.0.0.1:8901` 之类的基址挪进配置）：**不做**，理由是
    **不需要**——页面与接口同源（静态文件由同一个 `ui_server` 提供），请求全用相对路径
    （`/api/…`），换端口/换机器自动跟随，没有可外置的硬编码基址；真要跨机部署是
    反向代理/环境变量的事，不是页面配置项。`SSE`（`/api/events`）同理走相对路径。
- [x] **文档/导览（2026-09-12）**：原三项全部落到 `README.md` ——
  ① **协议版本↔页面功能映射** = 新增《能力总览》（13 项能力 × 实现文件 × 验收入口）+《页面一览》
  （11 页 × 作用 × 主要接口 × 存储）+《HTTP 接口清单》（GET/POST 全列；**契约仍以 `API.md` 为准**，
  这里只回答"有哪几个"）；② **黄金样本一键自检** = 剧本末尾写明 `run_checks.py` 覆盖哪些断言
  （`damm32=B`/`luhn32=E`/`mod97=21`、SN 边界 28 条、报文/帧边界、密钥生命周期、防 spoof 清单…）——
  **不新增"一键"入口**（测试台本身就是入口）；③ **demo 剧本**（入网→移动→走失→发现→定位→找回→结案）
  = 11 步表格，**按钮/文案按页面实测写**，并写明一处易错差别：首页走失表 `标记为走失` 只按 **SN**
  进表（**不立案**），`case.html` 的 `标记走失 / 立案` 才是以**人**为单位立案（名下设备→lost + 进表）。
  顺带修 README 两处过时数字（离线套件 7 → **9**）、标题（原自称 "L1/L2 Demo"），
  代码结构树补齐 19 个模块 + 11 页 + `pos.js`/`map.js`/共享 i18n（原来只有 L1–L3 时代的 12 个文件）。
  ⚠ **与 §六 的关系**：README《页面一览》是**入口摘要**，§六 是更细的存储审计 —— 两者同源，
  改任一处需同步另一处。

### 定位内核审查裁决记录（2026-09-13，`pos.js` / `track.html` / `test_posjs.py`）

- ✅ **修（真 bug，报告提了方向）：`samplesOf` 把「空表」与「没有这个数据源」混为一谈。**
  后端 `router_obs_range()` **明确会给每台站位一条序列、没数据就是空表**（`{S1:[…], S2:[], S3:[]}`），
  而两页原来都写 `(own && own.length) ? own : <设备流>` → **空表会回落到设备上报流**，
  等于把「**设备自己**到最近那台路由器的强度」当成**该站位**的测量：几台站位拿到同一份相关值，
  定位给出一个 **RMS/GDOP 都好看、但完全错的位置**（正是本项目一直在打的“退化输入给看着正常的错值”）。
  **实测复现**（浏览器，真实模式）：
  - 修复前 `{S1:200 条, S2:[], S3:[]}` → `obs.length = 3`，S2/S3 距离都等于设备流那条（30.2 m），
    估计点 (-1.9, 0.0)，站点表里 S2/S3 也显示着“观测 -75.0 dBm · router n=13”（编出来的）；
  - 修复后 → `obs.length = 1`，页面如实说「至少需要 2 个站位有观测」，站点表对应两行显示「—」。
  - **修法（判据单一源）**：`pos.js` 新增 `samplesFor(routerObs, sid, deviceSamples)`。
    有 per-station 序列 → 空表就是“没测到”返回 `[]`；**整个**序列都缺席（老数据源/旧后端）才回落到设备流
    （保留悬停窗口语义）。`track.html` / `replay.html`（含 `precomputeFrames` 的候选帧收集）全部改走它，
    `test_posjs.py` 加守卫断言两页都调 `samplesFor`（以前两页各写一份同样的三元，漂移了就会同刻不同位置）。
- ❌ **驳回（🟡）：`renderStations` 里 `stobs` 的「路由器」与 `n=` 之间缺空格。** 代码里**本来就**
  显式写了 `T("tk_st_src_router") + " "`（2026-09-12 那轮刚修过这里的双空格问题），实测渲染就是
  「-79.0 dBm · 路由器 n=13」（本轮浏览器快照可见）。
- ❌ **驳回（🟡：「`obs` 数组可能未排序 → `latestAt` 依赖升序」）**：① 接口**本来就**给升序
  （`tsdb.query_router_range` 是 `ORDER BY time ASC`）；② `latestAt` 自己也不依赖顺序 ——
  它遍历所有样本取 `t <= 目标` 中 **`t` 最大**的那条（注释里写明“乱序输入也不会取错”）。
  已补测试把这条保证锁住（升序/降序/乱序给出同一条）。
- 🟢 **不改（`condBefore` 巨大有限值判成 `layout` 而非 `collinear`）**：两者只差一个 CSS class，
  给出的坐标与改善量完全相同；已补测试把近共线（cond=1000）的**期望行为**写下来（仍给建议、
  仍要求离开直线、`reason=layout`），不再靠读代码去猜。
- 🟢 **不改（`kalmanTrack` 重置帧的 `cov` 用的是测量协方差 R）**：重置帧的“位置”就是该帧测量本身，
  R 正是它的协方差；后验 P 此时只反映重新播种的状态，反而会让人误以为“已经融合过了”。
  已在代码里补两行说明这个**有意**选择（原来只有“缺口 → 重置”一句）。
- 🟢 **不改（打点按钮禁用时加 tooltip / 距离环用 setRadius 而非重建）**：按钮禁用时同一行已显示
  「未设定 → 现在」= 窗口未收尾；距离环的**半径每帧都在变**（目标在动）→ 重建与 `setRadius` 等价，
  没有可省的工作量，而演示只有 3 台站位（按“没有测量之前不优化”不动）。

### 前端审查裁决记录（2026-09-12，三页联合审查）

- ✅ **修**：`track.html` 观测来源字符串的**优先级陷阱**——原写法
  `(src==="bind" ? A : (src==="router" ? B : "") + " n=" + o.n)` 让默认的 `window` 支输出成
  `"·  n=42"`（双空格）。实测三支为 `手动` / `路由器 n=42` / `·  n=42` → 改成
  `(src==="router" ? B + " " : "") + "n=" + o.n`，三支输出 `手动` / `路由器 n=42` / `n=42`。
  **审查说的「router 缺标签」「bind 缺 n=N」不成立**：router 一直有标签；`pos.js` 里
  `src==="bind"` 的 `n` **恒为 1**（`obsOfStation` 直接给 `n:1`），显示 n 是废话。
- ❌ **驳回：`rssi.html` `addRow` 的 `innerHTML` 是「XSS 缺陷」**。事实：① 三个调用点
  （`addRow(30,0)`、`-15,26`、`-15,-26`、按钮里的 `Math.round(...)`）**全是数字**，
  读回也一律 `parseFloat`，没有外部字符串进入该 sink；② **`track.html` 的 `addRow`
  是逐字相同的写法**（`title="${T("btn_del")}"` 也一样），不是「唯独 rssi 例外」；
  ③ 成文纪律只针对**空口来源**的值（`API.md` §metrics：`alg` 来自报文头 → 页面必须 `esc()`）。
  故当前**无注入源**，属"以后若接受外部字符串再改"的触发条件，不做无收益改写。
- ❌ **驳回：`rssi.html` 自带 `trilaterate` 是「重复实现/技术债」**。逐项对比：n≥3 的线性最小二乘
  （`2(xj−x0)`、`bb`、`A`/`b` 累加、`det<1e-12` 判奇异）与 `pos.js` **完全同式**；
  差异只有两处**有意为之**：① `n===2` 本页**返回两个交点**（教学要展示"两圆二义"），
  `pos.js` 取离上一次估计更近的那个（工程必须给唯一解）；② 本页不画 WLS/椭圆/GDOP。
  **同步义务**：若改 `pos.js` 的线性解，需回看本页（否则教学页会教旧公式）。
- ❌ **驳回：`simulate` 用 `Math.random()`「对比 A/n 不便」**。本页 `定位` 只吃**表里已显示**的
  RSSI，所以正确用法是「仿真一次 → 改 A/n → 按定位」，噪声保持不动即可公平对比，
  不需要种子（加"固定种子"复选框是功能新增，非缺陷）。
- ❌ **驳回：`track.html` `cx=310/cy=210` 硬编码**。画布就是 620×420（`L153/162` 与常量同处一屏），
  当前**完全正确**；无证据表明要改画布尺寸 → 不动（真要改尺寸时再一并提常量）。
- ❌ **驳回：`replay.html` `lastEvent`/`smoothAt` 每帧线性扫描**。无实测卡顿证据
  （回放窗口 ≤12 h、事件上限 2000）；按"没有测量之前不优化"不动。

### 服务端审查裁决记录（2026-09-13，`ui_server.py` 三点）

- ✅ **修（真 bug，报告指对了）**：`_api_truth` / `_api_replay` 用 `str.isdigit()` / `lstrip("-").isdigit()`
  **当整数校验** —— 它问的是"字符是不是数字类"，不是"`int()` 能不能解析"：
  `"--123".lstrip("-").isdigit()` 与 `"²".isdigit()`（上标二）**都为 True 但 `int()` 抛 ValueError**。
  `do_GET` **没有外层 try** → 异常直接冒到 socketserver → 连接线程崩、**客户端拿到「Failed to fetch」**
  （不是 400）。浏览器实测四条全中：`/api/truth?from=--123`、`?from=²`、`?step=²`、`/api/replay?from=²`
  （后三条报告没提，是同一根因）。修法：**唯一入口** `ui_server._int_arg(s, default)`（try/except int()），
  三处调用点全部换掉；语义保持（非法→回退默认值，`from` 负数仍可用；`to` 非法仍显式 `bad_param`）。
  回归：`test_server.TestUiQueryArgs` 4 条（行为表 + **AST 守卫「源码里不再有 `*.isdigit()` 调用」**）+
  浏览器复测四条 URL 全部 200、正常路径（负 from / step / minutes）不变。
  **同一处第 2 次被提（2026-09-13 下午）**：报告又建议改成 `replace("-", "").isdigit()` 或正则 `^-?\d+$`。
  实测两点：① **建议 A 是无效修法** —— `"--123".replace("-", "")` 仍是 `"123"` → `isdigit()` 仍为 True
  → `int("--123")` 照样抛，等于没修；② 建议 B（正则）能用，但会给"是不是整数"引入**第二套判据**，
  与 `int()` 不一致（`+5` / `" 42 "` / `1_000` / Unicode 数字 `٣` 都是 `int()` 能解析而 `^-?\d+$` 判否）。
  现实现 `_int_arg` 的定义就是**「`int()` 能解析什么就接受什么」**（唯一判据），故**维持不改**。
- ❌ **驳回：`_api_upload` 的 `finally: settimeout(None)` 会抛 `OSError` 盖掉 400 响应**。代码里
  **本来就**用 `try/except OSError: pass` 包住了（读的是同一份源码，评审漏看那两行）。顺手实测确认
  `close()` 后的 socket 调 `settimeout(None)` **确实抛 OSError** → 所以那层 except 不能删；已加断言锁住。
- ❌ **再驳回（同一指控第 2 次，2026-09-13 下午）：「`finally` 中仍有风险，建议把 `settimeout` 移到
  `try` 成功路径末尾」**。这次评审承认 `try/except OSError` 已有，只主张"仍有风险"。**真连接实测**
  （`TEMP/probe_upload.py`，原始 socket 打 `:8901`，两种场景：① 一个字节都不发 ② 先发半个请求体再停住）：
  - 两种场景都在 **10.0s** 后收到**完整** `400` 响应（`Content-Length` 带的 JSON 都在）→ 响应**没有被**
    `finally` 盖掉，指控的现象不存在；
  - 响应是 `HTTP/1.0`（`BaseHTTPRequestHandler.protocol_version` 默认值，本项目没改成 1.1）→ 服务端
    **不复用连接**（实测同连接再发请求 → `ConnectionAbortedError` 10053 = 对端已关）→ 也不存在
    "错位的请求体被当下一条请求解析"的问题。
  - **为什么 `finally` 才是对的**（评审的建议反而更差）：`settimeout` 是**套接字属性**，一旦不清就留在
    连接上；把它只写在 `try` 成功路径末尾，**超时分支就不会复位** —— 今天因 HTTP/1.0 随即关连接而无感，
    但若将来把 `protocol_version` 改成 `HTTP/1.1`（keep-alive 复用连接），上一个请求残留的 10s 读超时
    会把同一连接上的**后续请求**误杀。`finally` 保证"异常路径也复位"，是连接状态清理的正确位置。
  - 结论：**不改代码**（本轮 0 改动；`test_server.test_upload_finally_guards_settimeout` 已锁"防护存在"）。
- ✅ **修（措辞）**：上传读超时的报错原来写「无 Content-Length 且客户端未关流」，而 `settimeout`
  是**每次 recv 各算**（持续有数据就一直不超时）→ 改成「读取超时（无 Content-Length：需客户端关流
  结束上传；10 秒内没有收到数据即超时）」，把"怎么修 + 判据"都写出来。

### 前端/告警审查裁决记录（2026-09-13 第 2 轮，`app.js` / `index.html` / `alerts.py` / `energy.py`）

这一轮报告自己给的总评就是「无功能性 Bug、无安全漏洞、四文件均可直接合入」，全部条目都是 🟢 级。
逐条核实后 **0 处需要改代码**，其中 1 条（`alerts.py`）是把已有回退**读漏**导致的误判：

- ❌ **驳回：`alerts.py` `id_degraded` 里 `ts = int(r.get("ts_eff") or 0)` “会让缺时间戳的降级记录漏报”**。
  读漏的是**紧接着的下一行**：`worst[sn] = (lv, ts or now)` —— `ts=0` 只是**不参与窗口判定**
  （`if ts and now - ts > deg_sec` 为 False → 不 `continue`），告警**照样出**，`since` 回退到 `now`
  （评审建议的正是这个回退，代码里已有）。探针实测（`TEMP/probe_degraded.py`，六种输入）：

  | 输入 | 结果 |
  |---|---|
  | `ts_eff=NOW-5` | `id_degraded/warn`，since=设备时间 ✓ |
  | `ts_eff` **缺失** / `=0` / `=None` | `id_degraded/warn`，**since=now**（回退生效） |
  | `ts_eff=NOW-10⁶`（超窗） | 无告警（**有意**：超窗=“当前没问题了”自动消警） |
  | `accepted=False` | 无告警（**有意**：§8.3 只看已签接受的上报） |

  本轮**不改代码**，但补 8 条回归锁（`test_alerts.py`：L2/L3 × ts_eff 缺失/=0/None 都必报且
  `since==now`，外加超窗/被拒两条“有意不报”）—— 免得以后有人照这个误读把 `ts or now` 删成 `ts`。
- ❌ **驳回：`app.js` `renderRows` 每次轮询整体重排 tbody「可改用 `insertBefore` 只移动新行」**。
  无实测依据，且这是**把正确性绑死在插入路径上**的换法。实测（浏览器 50 行满，复刻同段排序跑 1000 次）：
  **0.10 ms/次轮询**（占 1s 轮询预算的 0.01%）→ 省下的是 0.1 毫秒/秒。反过来，这段排序是
  **「DOM 顺序 = 数据顺序」的唯一保证**：新行是 `appendChild` 到**末尾**的，而数据是最新在前，
  去掉排序新行就会沉到底下（清屏重连、seq 跳变时也会错位）。已在代码里写明这条**为什么留着**
  与实测数字（并注明“不值当为 0.1ms 换写法”）。
- ❌ **驳回/无法评估：`app.js` `fillSpoofKinds` / `spoofOptText` 的拼接**。报告这一段**文字被截断**
  （止于“`name` 来自后端 `o.en”），无法判定它具体主张什么；就代码本身核实：`o.kind` 与整个
  option 文案**都过了 `esc()`**（`<option value="${esc(o.kind)}">${esc(spoofOptText(o))}</option>`），
  且 `zh`/`en` 来自本仓库 `spoof.py` 的常量（不是空口/用户输入）→ 无注入源。
- ✅ **`index.html` / `energy.py`：报告说无问题，核实一致**（版本戳 `app.js?v=20260913d` 与
  文件一致；`energy.py` 参数在模块 docstring 标了“演示标定值，不是实测”）。

**结论**：本轮外部报告 0 真 bug（与最近几轮同样的形态：总评"可合入"，条目为风格/性能建议，
或把我已经写好的防护读漏）。处理方式仍然是**先复现、再改或带证据驳回**，并把误读点写成
测试锁或代码注释，避免同一个猜测反复回来。回归：`run_checks.py --e2e` **19/19**。

## 六、页面 × 存储接入现状（2026-09-11 审计）

| 页面 | 接口 | 存储 | 说明 |
|---|---|---|---|
| `registry.html` | `/api/registry`、`/api/upload` | **SQLite** `persons`/`devices` | 照片落 `ui/static/uploads/`（文件） |
| `case.html` | `/api/cases`、`/api/registry` | **SQLite** `cases`/`case_events` | |
| `track.html`（真实模式） | `/api/ts/query` | **IoTDB** `root.orpah.devices.<sn>` + `root.orpah.routers.<sid>.<sn>` | 模拟模式纯前端，无状态 |
| `track.html`（真实模式 · 定位站位） | `/api/stations` | **SQLite** `stations` | 站位坐标 / 时间窗 / 手动绑定；定位计算在前端（复用模拟模式内核） |
| `replay.html` | `/api/replay` + `/api/stations` | 只读 **IoTDB**（`devices.<sn>` + `routers.<sid>.<sn>` + `events`） | 按时间窗回放：定位逐帧用共享内核 `pos.js` 现算，不存回放结果 |
| `index.html` | `/api/status`、`/api/events`、`/api/ctl`、`/api/alerts`、`/api/ts/events` | **IoTDB** `root.orpah.events`（事件历史） | 拓扑计数/报文流环形仍是内存态（**重启归零**，属设计：当前状态展示）；告警**无存储**（每次按快照重算） |
| `case.html`（操作者输入框） | `/api/cases` + POST 带 `actor` | **localStorage** `orpah_ui_actor` → **IoTDB** `actor` 列 | 共享组件 `ui_i18n.js` 的 `bootActorBox()` |
| `keys.html` | `/api/keys` | **SQLite** `keys`/`key_revocations` | 密钥多代/宽限/退役/撤销；每个变更写 `key_*` 审计事件 |
| `sig.html` | `/api/sig` | 无（内存 KeyStore） | `KeyStore.save/load` 存在但 UI 未接；重启丢密钥（**仅签名算法演示**，与 `keys.html` 的正式密钥库无关） |
| `checksum.html` / `damm32.html` | `/api/checksum` | 无 | 纯计算，产出用完即弃 |
| `rssi.html` | 无 | 无 | 纯前端算法演示 |
| `attack.html`（攻击流量面板，2026-09-13） | `/api/status`（`spoof_kinds`/`spoof`）、`/api/ctl`（`spoof`/`spoof_reset`） | **IoTDB** `root.orpah.events`（每条攻击的裁决落 `id_report`/`id_reject`）；面板计数与「最近流量」是内存态（**重启归零**，同 `index.html` 的环形计数，属设计） | 攻击报文经**真空口链路**注入；面板**只装攻击报文**（按 nonce 认领，正常周期上报不进）；结果分三档：与期望一致 / 不一致 / **未等到结果**（`state=lost`） |

## 七、第三方素材与许可（2026-09-11 核实）

| 素材 | 位置 | 许可 | 义务 / 风险 |
|---|---|---|---|
| **Leaflet 1.9.4** | `ui/static/vendor/leaflet/`（已入库） | **BSD-2-Clause** | 保留版权与许可文本（已附 `LICENSE-leaflet.txt`）；可商用、无 copyleft |
| 底图瓦片 | 运行时外部请求，**不入库** | 各源不同 | 见 `track.html` 的 `TILE_PROVIDERS`；页面上选了哪个源就显示对应条款提示 |
| **离线网格底图** | `ui/static/map.js`（代码即素材，跟着代码入库） | **无第三方素材** | canvas 现画的网格/背景色，**不含任何第三方数据** → 不引入许可义务（这正是选它而不是随包分发瓦片的原因） |
| （将来）PMTiles 区域包 | 运行时本地文件，**不入库** | Protomaps basemap（数据源 OSM = ODbL） | 需自托管 + 署名；**不得**用 OSM 官方瓦片打包；做法与 Range 前置条件见 §二 |

**底图要点（选型时必读）**：

- `tile.openstreetmap.org`（默认）受独立的 [Tile Usage Policy](https://operations.osmfoundation.org/policies/tiles/) 约束：
  必须**可见署名**、必须**可识别 UA**（网页场景浏览器默认 UA 可接受）、必须**遵守缓存头**（≥7 天）；
  **禁止**批量下载 / 预取 / 离线打包 / 高倍自动扫图；**无 SLA，可无预警封禁**。
- **OpenTopoMap** = CC-BY-SA 3.0：可商用、免许可费；需按官方文本署名
  （`Map data: © OpenStreetMap contributors, SRTM | Map style: © OpenTopoMap (CC-BY-SA)`，
  不能简写），且衍生作品需相同方式共享。
- **CARTO**：**已从「瓦片源」列表移除** —— 其栅格端点现在必须 API key，实测整图返回
  `API KEY REQUIRED — carto.com/basemaps/apikey` 水印；且其地名也是当地语言，解决不了英文界面的地名问题。
- **Esri**：商业服务商，生产使用前需单独确认条款（可能需账号/付费）；World Imagery 是卫星影像、**无地名**。
- **底图地名语言**：栅格瓦片是预渲染图片，地名是像素，**没有 `lang` 参数** ——
  中国境内用 OSM/OpenTopoMap 必然是中文。故「瓦片源」列表**按界面语言给不同的一套**：
  中文界面街道图优先（地名中文是优点），英文界面把无地名的 Esri 置默认、带地名的标出 ⚠ 提醒。
- **离线场景（2026-09-12）**：本 demo **不含离线底图**（OSM 政策禁止；自建 PMTiles 需要额外数据包，
  用户定为「不下包」）。瓦片取不到时自动回落**本地自绘网格**（无第三方素材 → 无许可义务），
  并在图上明确提示「离线：底图取不到」+「重试底图」。
  真要英文地名只能走**矢量瓦片**（MapLibre GL + 样式指定 `name:en`）或自建栅格瓦片。
- **结论**：本库演示用法（网页内嵌 + attribution 控件 + 可自定义源）合规；
  但**产品化与离线场景不得依赖 OSM 官方瓦片**。

---

## 优先级

| 级别 | 项目 | 状态 |
|------|------|------|
| **P0** | 设备清册 + SN 状态（含多客户端绑定） | ✅ 完成 |
| **P0** | 走失案件闭环（基础） | ✅ 完成 |
| **P0** | 真实 report 聚合·阶段一（单无人机） | ✅ 完成（站位表 + 时间窗/手动绑定，见 §二） |
| **P0** | 审计日志（含操作者 + 保留期限 + 被拒上报可筛） | ✅ 完成 |
| **P0** | 告警红点（长未上报 / 走失超时 / 签名失败率） | ✅ 完成（通知渠道未做；~~`case_overtime` 恒定 crit 会把红点“恒亮挤没”~~ → **A 方案已解决**，见 §三） |
| **P1** | 密钥生命周期（**主体完成 2026-09-12**；CRL 分发**方向已定、按 §〇 只记录不实现**、存储保护=硬件信任根不在 demo 范围，均见 §一）、定位质量：残差/GDOP/条件数 + **加权 LS + 误差椭圆 + 搜索半径（2026-09-12 完成）** + **时序平滑·恒速卡尔曼（2026-09-12 完成，非滑动平均：无滞后 + 搜索半径用滤波后协方差）**、**回放（2026-09-12 完成：时间窗回放 + 轨迹/椭圆 + 事件时间线 + 无效时段标注/跳过 + 地图视图 + 轨迹导出 GPX/GeoJSON）**、离线地图（**基点导入/导出 2026-09-13 完成**；PMTiles 区域包用户定不下包、野外包切换 UI 未做）、**无认证防 spoof 演示（2026-09-12 主体完成：12 种攻击 + 空口端到端；**2026-09-13 补到 13 种攻击（新增格式非法、能力降级）+ 攻击流量独立面板 `attack.html`**；⚠ 暴露“路由器侧观测不入签名”开放问题）**、**限频 §5.8（2026-09-13 三层齐：服务端 per-SN + per-Router（验签之前）、Router 侧转发/探针两条、**设备侧自愿自限频**；`demo_ratelimit.py` + `test_ratelimit.py` + `test_selflimit.py`；⚠ per-SN 可被轮换 SN 绕过，见 §四）**、**真实 report 聚合·阶段二（模拟器侧已做 2026-09-12：`motion.py` 移动的人 + `routers.<sid>.<sn>` 序列 + `pos.js` 路由器序列分支；真机侧 2026-09-12 已备上机手册 + 自检脚本 `docs/real-hw-stage2.md` + `demo_hw1.py`，**未上机**，烧录由用户执行）**、**告警处置态/时效策略**（见 §三；**A+B+C 方案 2026-09-12 全部完成**：A = 案件加接手人 + 超时且无人接手才告警；B = 接手后仍超 24h 未发现再提醒；C = 按持续时长分级（no_report / case_handled_overtime 超升级阈值升 crit））** | ✅ **完成（2026-09-12/13）** —— 除下列「按 §〇 只记录不实现 / 待上机」者：CRL 分发（方向已定）、硬件信任根、阶段二真机联调（手册+脚本已备，**未上机**，烧录由用户执行）、路由器侧观测入签名（开放问题） |
| **P2** | 多租户权限、**案件经办/分配（见 §三）**、Wi-Fi 抓包对照、无源能耗模型、批量合规测试用例、隐私最小化/时钟可信深化、**回放视频联动（最多 4 路摄像头 + 地图视角，见 §五）**、**IPv6（Phase 1 只做 IPv4；未做不是缺口，见 §四；要上得一起改五处）** | — |

