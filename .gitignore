# 运行时上传的照片（本地上传，不随仓库提交）
ui/static/uploads/

# SQLite 数据库（运行时持久化，不随仓库提交）
# 连 rollback journal / WAL 临时文件一起忽略（事务中会短暂出现）
orpah.db
orpah.db-journal
orpah.db-wal
orpah.db-shm

# 本机临时排障脚本（不进仓库）
_e2e*.py

# 服务端本机运行日志（仓库里没有任何被跟踪的 .log）
*.log
